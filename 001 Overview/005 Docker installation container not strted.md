Отлично, мы нашли проблему! Контейнер запущен (`Up 11 minutes`), но внутри него **YSQL сервер не стартовал**. Логи показывают только общий успех `YugabyteDB Started`, но не показывают ключевую строку `YSQL listening on`.

Ваш последний запуск без флага `-d` подтвердил это — процесс "висит", потому что `yugabyted` не завершает старт всех сервисов.

### 🔧 Причина и быстрое решение

Причина в том, что для одноконтейнерного режима `yugabyted start` иногда требуется **больше памяти**, чем Docker предоставляет по умолчанию.

**Решение 1: Запуск с увеличенной памятью (рекомендую)**

Остановите текущий запуск (`Ctrl+C`), затем выполните:

```bash
docker run -d \
  --name yugabyte-single \
  -p 5433:5433 \
  -p 7000:7000 \
  -p 9001:9000 \
  --memory=2048m \
  --cpus=2 \
  yugabytedb/yugabyte:latest \
  bin/yugabyted start \
  --daemon=false \
  --ui=false
```

**Решение 2: Альтернативный метод — прямое подключение к уже работающему серверу**

Если `yugabyted` "висит", но процессы уже запущены, можно подключиться напрямую. В новом окне терминала выполните:

```bash
# Заходим в контейнер и подключаемся к локальному узлу
docker exec -it yugabyte-single bash
# Теперь внутри контейнера:
ysqlsh -h $(hostname -i) -p 5433 -U yugabyte
# или попробуйте:
ysqlsh -h 127.0.0.1 -p 5433 -U yugabyte
```

**Решение 3: Самый надежный способ — раздельный запуск Master и TServer**

Создайте файл `docker-compose.yml`:

```yaml
version: '3.8'
services:
  yb-master:
    image: yugabytedb/yugabyte:latest
    command: [ "/home/yugabyte/bin/yb-master",
              "--fs_data_dirs=/mnt/master",
              "--master_addresses=yb-master:7100",
              "--rpc_bind_addresses=yb-master:7100" ]
    ports:
      - "7000:7000"
    mem_limit: 1024m

  yb-tserver:
    image: yugabytedb/yugabyte:latest
    command: [ "/home/yugabyte/bin/yb-tserver",
              "--fs_data_dirs=/mnt/tserver",
              "--tserver_master_addrs=yb-master:7100",
              "--rpc_bind_addresses=yb-tserver:9100",
              "--enable_ysql=true" ]
    ports:
      - "5433:5433"
      - "9042:9042"
    depends_on:
      - yb-master
    mem_limit: 2048m
```

Запустите: `docker-compose up -d`

### 📊 Диагностика запущенных процессов

Если хотите понять, что происходит внутри контейнера, проверьте:

```bash
# Войдите в контейнер
docker exec -it yugabyte-single bash

# Проверьте запущенные процессы
ps aux | grep -E "(yb-|postgres)"

# Или проверьте логи YSQL напрямую
find /home/yugabyte/var/logs -name "*.log" -exec grep -l "YSQL\|postgres" {} \;
cat /home/yugabyte/var/logs/ysql.log 2>/dev/null || echo "Файл лога не найден"
```

### 💡 Краткий план действий

1.  **Нажмите `Ctrl+C`** в терминале, где "висит" `yugabyted`.
2.  **Удалите старый контейнер**: `docker rm -f yugabyte-single`
3.  **Запустите с увеличенной памятью** (Решение 1) или **используйте docker-compose** (Решение 3).
4.  **Проверьте логи на предмет `YSQL listening`**: `docker logs yugabyte-single | tail -20`
