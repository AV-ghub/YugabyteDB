А, теперь понятно! Вы используете **другой файл docker-compose.yml** — не тот, что я предлагал ранее, а более сложный с тремя мастерами и одним TServer (кластер из 4 узлов). Это отличный вариант!

В этом случае имя контейнера — **`yugabyte-yb-tserver-n1-1`**, а не просто `yb-tserver-n1`. Docker Compose формирует имя по шаблону: **`<папка_проекта>_<имя_сервиса>_<номер>`**.

### 🔧 Правильные команды для вашего кластера

**1. Войти в контейнер TServer:**
```bash
docker exec -it yugabyte-yb-tserver-n1-1 bash
```

**2. Подключиться к YSQL внутри контейнера:**
```bash
# Внутри контейнера
ysqlsh -U yugabyte -h yb-tserver-n1
# или просто
ysqlsh -U yugabyte
```

**3. Прямое подключение с вашего компьютера (рекомендуется):**
Так как порт `5433` проброшен на хост, вы можете подключаться напрямую:
```bash
psql -h localhost -p 5433 -U yugabyte
```
*(Если `psql` не установлен: `sudo apt install postgresql-client`)*

### 📊 Структура вашего кластера

Из вывода видно, что у вас запущено:
- **3 Master-узла** (`yb-master-n1-1`, `yb-master-n2-1`, `yb-master-n3-1`) — для управления метаданными
- **1 TServer-узел** (`yugabyte-yb-tserver-n1-1`) — для хранения данных и обработки запросов

**Порт 5433** проброшен только с контейнера `yugabyte-yb-tserver-n1-1` (последняя строка: `0.0.0.0:5433->5433/tcp`), поэтому подключение нужно делать к нему.

### 🌐 Веб-интерфейс
Веб-интерфейс доступен по адресу: **`http://localhost:7000`** (проброшен с первого master-узла).

### ✅ Быстрая проверка
Выполните в терминале (на хосте, не в контейнере):
```bash
# Проверка подключения
psql -h localhost -p 5433 -U yugabyte -c "SELECT version();"
```
Должна вернуться информация о YugabyteDB.

### 🔍 Если нужно посмотреть другие контейнеры
```bash
# Список всех контейнеров с именами
docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"

# Войти в master-узел (если нужно)
docker exec -it yugabyte-yb-master-n1-1 bash
```

**Попробуйте подключиться через `psql -h localhost -p 5433 -U yugabyte` и скажите, получилось ли?** Это самый удобный способ для работы с базой.
