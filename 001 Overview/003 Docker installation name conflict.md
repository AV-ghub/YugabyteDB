### 🔧 Исправляем ошибку с именем контейнера

Выполните эти команды по порядку:

```bash
# 1. Останавливаем старый контейнер (если он еще висит)
docker stop yugabyte-single

# 2. Удаляем старый контейнер
docker rm yugabyte-single

# 3. Пробуем запустить снова с новым портом 9001
docker run -d \
  --name yugabyte-single \
  -p 5433:5433 \
  -p 7000:7000 \
  -p 9001:9000 \
  yugabytedb/yugabyte:latest \
  bin/yugabyted start \
  --daemon=false \
  --ui=false
```

### ✅ Проверяем запуск

После выполнения команды `docker run` проверьте:

```bash
# Смотрим список контейнеров (должен быть в статусе "Up")
docker ps

# Если контейнер не запустился, смотрим логи
docker logs yugabyte-single
```

### 🎯 Краткая шпаргалка по частым командам Docker

| Действие | Команда |
|----------|---------|
| **Посмотреть все контейнеры (включая остановленные)** | `docker ps -a` |
| **Остановить контейнер** | `docker stop <имя_или_id>` |
| **Удалить контейнер** | `docker rm <имя_или_id>` |
| **Удалить контейнер принудительно (если не останавливается)** | `docker rm -f <имя_или_id>` |
| **Посмотреть логи** | `docker logs <имя_или_id>` |
| **Подключиться к YSQL** | `docker exec -it yugabyte-single bin/ysqlsh -U yugabyte` |

### 📊 Если порт 7000 тоже занят

Если при повторном запуске появится ошибка с портом 7000, проверьте, что предыдущий контейнер действительно удалился:

```bash
# Убедимся, что контейнера больше нет
docker ps -a | grep yugabyte

# Если есть другие контейнеры, мешающие работе
docker ps -a --format "table {{.ID}}\t{{.Names}}\t{{.Ports}}"
```

Если порт 7000 занят другим процессом, можно использовать аналогичный подход:

```bash
# Останавливаем и удаляем старый контейнер
docker rm -f yugabyte-single

# Запускаем с другим портом для веб-интерфейса
docker run -d \
  --name yugabyte-single \
  -p 5433:5433 \
  -p 7001:7000 \  # Изменили внешний порт для веб-интерфейса
  -p 9001:9000 \
  yugabytedb/yugabyte:latest \
  bin/yugabyted start \
  --daemon=false \
  --ui=false
```

**После успешного запуска** веб-интерфейс будет доступен по адресу `http://localhost:7001` (если меняли порт).

### 🚀 Что делать дальше

1. **Проверьте статус контейнера**: `docker ps`
2. **Если запустился успешно** — подключитесь к базе:
   ```bash
   docker exec -it yugabyte-single bin/ysqlsh -U yugabyte
   ```
3. **Выполните тестовые SQL-запросы** из предыдущего сообщения
