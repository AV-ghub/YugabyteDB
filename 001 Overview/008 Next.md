Превосходно! 🎉 

Вы успешно запустили **распределенный кластер YugabyteDB** (3 мастера + 1 TServer) и подключились к нему. Теперь у вас есть полностью рабочая среда для изучения.

### 🚀 Что можно исследовать дальше в вашем кластере:

1.  **Веб-интерфейс** (`http://localhost:7000`) — посмотрите, как отображаются все 4 узла, таблицы, метрики производительности.

2.  **Работа с данными** — создайте несколько таблиц, вставьте данные:
    ```sql
    CREATE TABLE users (id SERIAL PRIMARY KEY, name TEXT, email TEXT);
    INSERT INTO users (name, email) VALUES ('Иван', 'ivan@test.ru'), ('Мария', 'maria@test.ru');
    SELECT * FROM users;
    ```

3.  **Проверка распределенности** — посмотрите, как данные распределяются:
    ```sql
    -- В веб-интерфейсе на вкладке "Tables" вы увидите, как таблица разбита на таблеты (tablets)
    ```

4.  **Тестирование отказоустойчивости** (опционально) — остановите один мастер-контейнер и посмотрите, как кластер продолжает работать:
    ```bash
    docker stop yugabyte-yb-master-n2-1
    ```
    Затем проверьте веб-интерфейс и подключение к YSQL — всё должно продолжать работать.

### 💡 Следующие шаги в изучении архитектуры:

Когда будете готовы углубиться, можно изучить:
- **Как работает шардирование** (автоматическое разбиение данных на таблеты)
- **Механизм репликации Raft** (как данные сохраняются на нескольких узлах)
- **Распределенные транзакции** (как YugabyteDB обеспечивает ACID-гарантии в кластере)
