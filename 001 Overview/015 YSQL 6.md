**Отлично! Прогресс есть — теперь Postgres запущен и версия определяется!** Проблема в том, что **системный каталог YSQL не полностью синхронизирован с мастерами**.

### 🔍 Диагностика по выводу:

1. ✅ **Postgres запущен** на порту 5433 (`0.0.0.0:5433`)
2. ✅ **Базы данных существуют** (`yugabyte`, `postgres`, `system_platform`)
3. ✅ **YSQL работает** (`SELECT version()` успешно)
4. ❌ **Системная таблица `system.transactions` недоступна** — это означает, что YSQL системный каталог не инициализирован в распределенном хранилище

### 🛠️ Решение: принудительная инициализация через yb-admin

Выполните эту последовательность команд:

```bash
# 1. Проверяем статус мастеров
docker exec yugabyte-yb-master-n1-1 yb-admin --master_addresses yb-master-n1:7100,yb-master-n2:7100,yb-master-n3:7100 list_all_masters

# 2. Пробуем команду для более новых версий (может сработать)
docker exec yugabyte-yb-master-n1-1 yb-admin --master_addresses yb-master-n1:7100,yb-master-n2:7100,yb-master-n3:7100 ysql_catalog_version

# 3. Если вторая команда работает, сбрасываем каталог (вам нужно имя базы)
docker exec yugabyte-yb-master-n1-1 yb-admin --master_addresses yb-master-n1:7100,yb-master-n2:7100,yb-master-n3:7100 delete_namespace ysql.yugabyte

# 4. Перезапускаем TServer
docker-compose restart yb-tserver-n1

# 5. Ждем 2 минуты
sleep 120

# 6. Пробуем создать таблицу
psql -h localhost -p 5433 -U yugabyte -c "CREATE TABLE test_simple (id INT PRIMARY KEY);"
```

### 🎯 Альтернативное решение: ручное исправление через мастеров

Если вышеуказанное не сработает, попробуйте этот подход:

```bash
# 1. Останавливаем TServer
docker-compose stop yb-tserver-n1

# 2. Заходим в контейнер мастера и смотрим системные таблицы
docker exec -it yugabyte-yb-master-n1-1 bash

# Внутри контейнера мастера:
yb-admin --master_addresses yb-master-n1:7100,yb-master-n2:7100,yb-master-n3:7100 list_namespaces

# Если увидите что-то вроде `ysql.yugabyte`, удалите:
yb-admin --master_addresses yb-master-n1:7100,yb-master-n2:7100,yb-master-n3:7100 delete_namespace ysql.yugabyte

# Выходим
exit

# 3. Полностью удаляем данные TServer
docker-compose down yb-tserver-n1
docker volume rm $(docker volume ls -q | grep yugabyte) 2>/dev/null || true

# 4. Перезапускаем только TServer
docker-compose up -d yb-tserver-n1

# 5. Ждем 3 минуты
sleep 180

# 6. Пробуем
psql -h localhost -p 5433 -U yugabyte -c "CREATE TABLE final_test (id INT PRIMARY KEY);"
```

### 📊 Понимание проблемы

Ошибка `system.transactions not found` возникает потому что:
1. **YSQL локально инициализирован** (есть `pg_data_15`, Postgres запущен)
2. **Но распределенные системные таблицы не созданы** в DocDB (хранилище YugabyteDB)
3. При попытке создать таблицу YSQL пытается начать **распределенную транзакцию**
4. Для этого нужна таблица `system.transactions`, которой нет

### ⚡ Быстрая проверка-обход

Попробуйте создать таблицу **без первичного ключа** (это иногда обходит проблему с системными таблицами):

```bash
psql -h localhost -p 5433 -U yugabyte -c "CREATE TABLE test_nopk (id INT);"
psql -h localhost -p 5433 -U yugabyte -c "\dt"
```

### ✅ Рекомендация

**Сначала попробуйте самый простой вариант:**

```bash
# Просто пересоздаем TServer с чистого листа
docker-compose stop yb-tserver-n1
docker-compose rm -f yb-tserver-n1
docker-compose up -d yb-tserver-n1
sleep 150  # Ждем 2.5 минуты!
psql -h localhost -p 5433 -U yugabyte -c "CREATE TABLE test1 (id INT);"
```

**Если это не сработает — нужно удалять namespace через yb-admin.**

**Какой вариант пробуете первым?** Сообщите о результате каждой команды, особенно `list_namespaces` и `delete_namespace` если они будут доступны.
