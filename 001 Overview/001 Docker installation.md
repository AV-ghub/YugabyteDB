### 📋 Предварительные требования
*   **Операционная система**: Linux, macOS или Windows 10/11 (с WSL2 для Windows).
*   **Docker**: версия 20.10+ и Docker Compose v2+.
*   **Порты**: Убедитесь, что на хосте свободны порты `5433`, `7000`, `9000`.
*   **Ресурсы**: Минимум 2-4 ГБ свободной оперативной памяти для одного узла.

### 🚀 Пошаговая инструкция: от установки до первого запроса

#### **Шаг 1: Установка и проверка Docker**
Если Docker не установлен, выполните следующие команды (для Ubuntu/Debian):

```bash
# Обновление пакетов и установка Docker
sudo apt-get update
sudo apt-get install -y docker.io docker-compose

# Проверка установки
docker --version
docker-compose --version

# Добавление текущего пользователя в группу docker (чтобы не использовать sudo)
sudo usermod -aG docker $USER
# После этой команды нужно перезапустить сессию терминала или выйти/войти в систему
```

> Для других ОС следуйте официальным инструкциям: [Docker Desktop](https://www.docker.com/products/docker-desktop/)

#### **Шаг 2: Запуск однопользовательского кластера (самый простой способ)**
Этот метод использует официальный образ `yugabytedb/yugabyte`, который объединяет все процессы в одном контейнере:

```bash
# Запуск контейнера с YugabyteDB
docker run -d \
  --name yugabyte-single \
  -p 5433:5433 \
  -p 7000:7000 \
  -p 9000:9000 \
  yugabytedb/yugabyte:latest \
  bin/yugabyted start \
  --daemon=false \
  --ui=false
```

**Что происходит:**
*   `-p 5433:5433` — пробрасывает порт для YSQL (PostgreSQL-совместимый API)
*   `-p 7000:7000` — порт веб-интерфейса
*   `-p 9000:9000` — порт для yb-tserver
*   `--daemon=false` — запуск в foreground режиме (удобно для логов)

#### **Шаг 3: Проверка работоспособности**
1.  **Проверьте запущенные контейнеры**:
    ```bash
    docker ps
    ```
    Вы должны увидеть контейнер `yugabyte-single` в статусе "Up".

2.  **Проверьте логи** (опционально):
    ```bash
    docker logs yugabyte-single
    ```
    В логах ищите строки `YSQL listening on 0.0.0.0:5433` и `Web server started on`.

3.  **Откройте веб-интерфейс**: Перейдите в браузере по адресу `http://localhost:7000`. Вы увидите дашборд с информацией о кластере, узлах и таблицах.

#### **Шаг 4: Подключение и первый запрос**
1.  **Подключитесь к базе через psql**:
    ```bash
    docker exec -it yugabyte-single bin/ysqlsh -h localhost -U yugabyte
    ```
    По умолчанию пароль не требуется.

2.  **Выполните тестовые команды**:
    ```sql
    -- Создайте тестовую таблицу
    CREATE TABLE IF NOT EXISTS test_table (
        id SERIAL PRIMARY KEY,
        name VARCHAR(100),
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
    );

    -- Вставьте данные
    INSERT INTO test_table (name) VALUES ('Первая запись'), ('Вторая запись');

    -- Прочитайте данные
    SELECT * FROM test_table;

    -- Проверьте системные таблицы
    SELECT * FROM yb_servers();
    ```

### 🎯 Альтернативный способ: Запуск полноценного многоузлового кластера через Docker Compose
Для более реалистичного тестирования распределенных возможностей можно развернуть кластер из 3 узлов:

```yaml
# docker-compose.yaml
version: '3.8'

services:
  yb-master-n1:
    image: yugabytedb/yugabyte:latest
    command: [ "/home/yugabyte/bin/yb-master",
              "--fs_data_dirs=/mnt/master",
              "--master_addresses=yb-master-n1:7100,yb-master-n2:7100,yb-master-n3:7100",
              "--rpc_bind_addresses=yb-master-n1:7100",
              "--server_broadcast_addresses=yb-master-n1:7100" ]
    ports:
      - "7000:7000"

  yb-master-n2:
    image: yugabytedb/yugabyte:latest
    command: [ "/home/yugabyte/bin/yb-master",
              "--fs_data_dirs=/mnt/master",
              "--master_addresses=yb-master-n1:7100,yb-master-n2:7100,yb-master-n3:7100",
              "--rpc_bind_addresses=yb-master-n2:7100",
              "--server_broadcast_addresses=yb-master-n2:7100" ]

  yb-master-n3:
    image: yugabytedb/yugabyte:latest
    command: [ "/home/yugabyte/bin/yb-master",
              "--fs_data_dirs=/mnt/master",
              "--master_addresses=yb-master-n1:7100,yb-master-n2:7100,yb-master-n3:7100",
              "--rpc_bind_addresses=yb-master-n3:7100",
              "--server_broadcast_addresses=yb-master-n3:7100" ]

  yb-tserver-n1:
    image: yugabytedb/yugabyte:latest
    command: [ "/home/yugabyte/bin/yb-tserver",
              "--fs_data_dirs=/mnt/tserver",
              "--tserver_master_addrs=yb-master-n1:7100,yb-master-n2:7100,yb-master-n3:7100",
              "--rpc_bind_addresses=yb-tserver-n1:9100",
              "--server_broadcast_addresses=yb-tserver-n1:9100",
              "--ysql_enable_auth=false" ]
    ports:
      - "5433:5433"
    depends_on:
      - yb-master-n1
      - yb-master-n2
      - yb-master-n3
```

**Запуск кластера:**
```bash
docker-compose up -d
# Проверка состояния
docker-compose ps
```

### 🛠️ Основные команды для управления

| Действие | Команда |
|----------|---------|
| **Остановка контейнера** | `docker stop yugabyte-single` |
| **Запуск контейнера** | `docker start yugabyte-single` |
| **Удаление контейнера** | `docker rm -f yugabyte-single` |
| **Просмотр логов** | `docker logs -f yugabyte-single` |
| **Вход в контейнер** | `docker exec -it yugabyte-single bash` |
| **Подключение YSQL** | `docker exec -it yugabyte-single ysqlsh -U yugabyte` |

### 🔍 Что проверить после установки

1.  **Веб-интерфейс** (`http://localhost:7000`):
    *   Количество узлов в кластере
    *   Статус таблетов (tablets)
    *   Производительность и метрики

2.  **Подключение извне контейнера**:
    ```bash
    # Используя psql на хосте
    psql -h localhost -p 5433 -U yugabyte
    ```

3.  **Тестирование базовых операций**:
    *   Создание базы данных и таблиц
    *   Вставка и выборка данных
    *   Проверка репликации (для многоузлового кластера)

### ⚠️ Возможные проблемы и решения

| Проблема | Решение |
|----------|---------|
| **Порт уже занят** | Измените порт в команде `-p НОВЫЙ_ПОРТ:5433` |
| **Недостаточно памяти** | Увеличьте лимит памяти в Docker Desktop или освободите ресурсы |
| **Ошибка подключения к YSQL** | Убедитесь, что контейнер запущен (`docker ps`) и порт проброшен |
| **Ошибки при запуске кластера** | Проверьте логи: `docker logs [имя_контейнера]` |

### 📈 Следующие шаги

После успешного запуска рекомендую:
1.  Изучить веб-интерфейс и его вкладки
2.  Создать тестовую базу данных и несколько таблиц
3.  Попробовать вставить несколько тысяч записей и протестировать производительность
4.  Для кластера из 3 узлов: отключить один узел и проверить, как система продолжает работать

