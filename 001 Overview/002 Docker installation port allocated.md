### 🔧 Решение проблемы с занятым портом

У вас есть два простых варианта:

#### **Вариант 1 (Проще всего): Использовать другой порт**
Замените `-p 9000:9000` на любой свободный высокий порт, например `-p 9001:9000`:
```bash
docker run -d \
  --name yugabyte-single \
  -p 5433:5433 \
  -p 7000:7000 \
  -p 9001:9000 \
  yugabytedb/yugabyte:latest \
  bin/yugabyted start \
  --daemon=false \
  --ui=false
```
В этом случае внешний порт `9001` на вашей машине будет связан с внутренним портом `9000` контейнера. Для YSQL (`5433`) и веб-интерфейса (`7000`) это не критично.

#### **Вариант 2: Освободить порт 9000 (если он вам не нужен)**
Сначала узнайте, какой процесс его использует:
```bash
sudo lsof -i :9000
# или
sudo ss -tulpn | grep :9000
```
Затем вы можете остановить этот процесс (если он не критичен) или выбрать Вариант 1.

---

### 🚀 После успешного запуска

После того как контейнер запустится (проверьте командой `docker ps`), выполните стандартные шаги проверки:

1.  **Подключитесь через YSQL**:
    ```bash
    docker exec -it yugabyte-single bin/ysqlsh -U yugabyte
    ```

2.  **Выполните тестовые команды**:
    ```sql
    -- Создайте тестовую таблицу
    CREATE TABLE IF NOT EXISTS test_table (
        id SERIAL PRIMARY KEY,
        name VARCHAR(100)
    );

    -- Вставьте данные
    INSERT INTO test_table (name) VALUES ('Тестовая запись из Docker');

    -- Прочитайте данные
    SELECT * FROM test_table;
    ```

3.  **Откройте веб-интерфейс** в браузере: `http://localhost:7000`

### 💡 Если снова возникнет ошибка с портом
Помните, что нужно проверять **все три порта** (`5433`, `7000`, `9000` или его замена). Если занят другой, используйте ту же логику — измените первый номер в паре `-p ВАШ_ПОРТ:ПОРТ_В_КОНТЕЙНЕРЕ`.

### ❓ Что дальше?
Как только кластер запустится, давайте поэкспериментируем:
1.  Проверим, как данные распределяются внутри
2.  Попробуем простые операции на нескольких таблицах
3.  Посмотрим, как отображаются метрики в веб-интерфейсе
