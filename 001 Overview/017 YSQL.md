Отлично, теперь проблема ясна! Ошибка `system.transactions not found` возникает даже при создании базы данных, что указывает на **фундаментальную проблему с инициализацией системного каталога YSQL**. 

Давайте решим это окончательно. Проблема в том, что **YSQL системные таблицы не созданы в распределенном хранилище**.

### 🛠️ Окончательное решение: Принудительная инициализация через создание системных таблиц

Выполните эту последовательность **по порядку**:

```bash
# 1. Полностью останавливаем кластер
docker compose down -v

# 2. Удаляем ВСЕ данные
docker volume prune -f
docker system prune -f

# 3. Проверяем, что ничего не осталось
docker ps -a | grep yugabyte
docker volume ls | grep yugabyte

# 4. Создаем УПРОЩЕННЫЙ docker-compose.yml
cat > docker-compose.yml << 'EOF'
version: '3.8'

services:
  yb-master:
    image: yugabytedb/yugabyte:latest
    command:
      - "/home/yugabyte/bin/yb-master"
      - "--fs_data_dirs=/mnt/master"
      - "--master_addresses=yb-master:7100"
      - "--rpc_bind_addresses=yb-master:7100"
    mem_limit: 1024m
    ports:
      - "7000:7000"

  yb-tserver:
    image: yugabytedb/yugabyte:latest
    command:
      - "/home/yugabyte/bin/yb-tserver"
      - "--fs_data_dirs=/mnt/tserver"
      - "--tserver_master_addrs=yb-master:7100"
      - "--rpc_bind_addresses=yb-tserver:9100"
      - "--enable_ysql"
      - "--ysql_enable_auth=false"
      - "--pgsql_proxy_bind_address=0.0.0.0:5433"
      - "--ysql_num_shards_per_tserver=1"      # Уменьшаем для тестов
      - "--replication_factor=1"               # Только для локального тестирования!
    ports:
      - "5433:5433"
    depends_on:
      - yb-master
    mem_limit: 2048m
EOF

# 5. Запускаем ТОЛЬКО master и ждем его готовности
docker compose up -d yb-master
echo "Ждем запуска мастера... (30 секунд)"
sleep 30

# 6. Проверяем, что мастер работает
docker compose logs yb-master | grep -i "running\|started" | tail -5

# 7. Запускаем TServer
docker compose up -d yb-tserver
echo "Ждем полной инициализации YSQL... (90 секунд)"
sleep 90

# 8. Проверяем логи TServer
docker compose logs yb-tserver | grep -i "ysql\|postgres\|5433" | tail -10
```

### 🔍 Критически важный шаг: Проверка инициализации системных таблиц

После запуска выполните:

```bash
# 1. Проверяем, созданы ли системные таблицы
docker exec yugabyte-yb-master-1 yb-admin --master_addresses yb-master:7100 list_namespaces

# Должны увидеть системные пространства имен:
# system, system_schema, system_auth (YCQL)
# И YSQL пространства: template0, template1, postgres

# 2. Если YSQL пространств нет, создаем их вручную
docker exec yugabyte-yb-tserver-1 bash -c "
  # Проверяем локальные файлы YSQL
  ls -la /mnt/tserver/pg_data_15/ 2>/dev/null || echo 'Нет pg_data_15'
  
  # Перезапускаем YSQL процесс
  pkill -f postgres 2>/dev/null || true
  sleep 5
"

# 3. Пробуем подключиться через другую базу
psql -h localhost -p 5433 -U yugabyte -d postgres -c "SELECT 1 as test;"
```

### 🎯 Альтернативный подход: Используем yugabyted для управления

Если упрощенный docker-compose не сработает, давайте используем `yugabyted` который правильно инициализирует всё:

```bash
# 1. Очищаем всё
docker compose down -v
docker volume prune -f

# 2. Запускаем yugabyted в отдельном контейнере
docker run -d \
  --name yb-cluster \
  -p 5433:5433 \
  -p 7000:7000 \
  -p 9000:9000 \
  --memory=4096m \
  --cpus=2 \
  yugabytedb/yugabyte:latest \
  bin/yugabyted start \
  --daemon=false \
  --ui=false \
  --listen=0.0.0.0 \
  --advertise_address=0.0.0.0

# 3. Ждем 2 минуты
sleep 120

# 4. Проверяем
psql -h localhost -p 5433 -U yugabyte -c "CREATE DATABASE testdb;"
psql -h localhost -p 5433 -U yugabyte -d testdb -c "CREATE TABLE test (id INT PRIMARY KEY);"
```

### 📊 Почему это происходит: Архитектурная причина

Ошибка `system.transactions not found` означает:
1. **YSQL процесс запущен** (Postgres на порту 5433)
2. **Но распределенные системные таблицы не созданы** в DocDB
3. Эти таблицы создаются при **первой инициализации YSQL** через связку Master-TServer
4. Если Master не готов или TServer запускается слишком быстро — инициализация ломается

**Ключевые параметры которые мы исправили:**
- `--replication_factor=1` — для локального тестирования
- `--ysql_num_shards_per_tserver=1` — уменьшает нагрузку
- Последовательный запуск (сначала Master, потом TServer)
- Увеличенное время ожидания

### ✅ Рекомендация

**Попробуйте сначала упрощенный docker-compose вариант** (с 1 Master + 1 TServer). Он проще и чаще работает.

**Сообщите, что показывает команда `docker exec yugabyte-yb-master-1 yb-admin --master_addresses yb-master:7100 list_namespaces` после запуска.** Это скажет нам, созданы ли системные пространства имен YSQL.
