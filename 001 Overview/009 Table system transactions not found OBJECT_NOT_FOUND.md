```bash
andrey@andrey-VirtualBox:~$ psql -h localhost -p 5433 -U yugabyte
psql (16.0 (Ubuntu 16.0-1.pgdg20.04+1), server 15.12-YB-2.25.2.0-b0)
Type "help" for help.

yugabyte=# CREATE TABLE users (id SERIAL PRIMARY KEY, name TEXT, email TEXT);
ERROR:  Table system.transactions not found: OBJECT_NOT_FOUND

```
Это **нормальное поведение** для только что запущенного распределенного кластера YugabyteDB. Ошибка возникает потому, что системные таблицы еще не полностью инициализированы во всех узлах кластера.

### 🔍 Причина и простое решение

Когда вы запускаете кластер через docker-compose, **мастер-узлы и TServer запускаются почти одновременно**. TServer может попытаться создать таблицу до того, как мастер-узлы полностью синхронизируют системный каталог между собой (через механизм Raft консенсуса).

**Решение: подождите 30-60 секунд** после запуска кластера и повторите попытку.

### ✅ Практические шаги

1.  **Подождите минуту** после `docker-compose up -d`.

2.  **Проверьте статус мастер-узлов** в веб-интерфейсе `http://localhost:7000`. На главной странице или в разделе **Masters** все три мастера должны иметь статус **Alive** (а не просто быть в списке).

3.  **Попробуйте создать таблицу еще раз:**
    ```sql
    CREATE TABLE users (id SERIAL PRIMARY KEY, name TEXT, email TEXT);
    ```

4.  **Если ошибка сохраняется**, выполните простой запрос для "прогрева" системного каталога:
    ```sql
    -- Этот запрос заставит TServer обратиться к мастер-узлам за метаданными
    SELECT * FROM system.local;
    -- Подождите 10 секунд и повторите CREATE TABLE
    ```

### 🛠️ Альтернативное решение: принудительная инициализация

Если проблема не исчезает, можно перезапустить TServer:

```bash
# Перезапускаем только TServer-контейнер
docker-compose restart yb-tserver-n1
# Ждем 30 секунд и пробуем снова
```

### 📊 Как проверить готовность кластера

Вот команды для проверки состояния:

```bash
# Проверить логи мастер-узла (должны быть строки о выборах лидера)
docker logs yugabyte-yb-master-n1-1 | grep -i "leader\|election\|consensus"

# Проверить, что все мастера в кворуме
docker exec yugabyte-yb-master-n1-1 yb-admin --master_addresses yb-master-n1:7100,yb-master-n2:7100,yb-master-n3:7100 list_all_masters
```

### 💡 Профилактика на будущее

При запуске кластера добавьте небольшую задержку перед началом работы:

```bash
# Вместо immediate использования
docker-compose up -d
sleep 90  # Ждем 90 секунд для полной инициализации
psql -h localhost -p 5433 -U yugabyte -c "CREATE TABLE test (id int);"
```

### 🎯 Что происходит на самом деле

Ошибка `system.transactions not found` указывает, что:
1. TServer пытается начать транзакцию для создания таблицы
2. Для этого ему нужна системная таблица `system.transactions`
3. Эта таблица еще не реплицирована на все мастер-узлы
4. Кворум мастеров (2 из 3) еще не готов обслуживать запрос

**Это стандартное поведение распределенных систем с консенсусом** — все узлы должны договориться о состоянии системы перед началом работы.

**Подождите минуту и попробуйте снова выполнить `CREATE TABLE`. Получилось?** Если проблема сохраняется дольше 2-3 минут, стоит проверить логи мастер-узлов на предмет ошибок консенсуса.
