### 🔍 Как попасть в контейнеры с docker-compose

1.  **Узнайте имена контейнеров**:
    ```bash
    docker-compose ps
    ```
    Вы увидите два контейнера с именами типа `yugabyte_yb-master_1` и `yugabyte_yb-tserver_1`.

2.  **Войти в контейнер TServer** (где работает YSQL):
    ```bash
    docker exec -it yugabyte_yb-tserver_1 bash
    ```

3.  **После входа подключитесь к YSQL**:
    ```bash
    # Внутри контейнера yb-tserver
    ysqlsh -U yugabyte
    ```

### 🚀 Прямое подключение к YSQL из вашей системы

Также вы можете подключаться **напрямую с хоста**, без входа в контейнер, потому что порт `5433` проброшен:

```bash
# Подключение с вашего компьютера (хоста)
psql -h localhost -p 5433 -U yugabyte
```

**По умолчанию пароля нет**, просто нажмите Enter. Если `psql` не установлен, установите его:

```bash
# Для Ubuntu/Debian
sudo apt-get install postgresql-client

# Для macOS
brew install libpq
```

### 📊 Сравнение вариантов подключения

| Способ | Команда | Преимущества |
|--------|---------|--------------|
| **Вход в контейнер + ysqlsh** | `docker exec -it yugabyte_yb-tserver_1 bash`, затем `ysqlsh -U yugabyte` | Прямой доступ к окружению БД |
| **Прямое подключение с хоста** | `psql -h localhost -p 5433 -U yugabyte` | Удобнее, знакомый `psql`, работа с локальными файлами |
| **Веб-интерфейс** | Откройте `http://localhost:7000` в браузере | Визуальный мониторинг, метрики, управление |

### 🔧 Если имя контейнера отличается

Если `docker exec -it yugabyte_yb-tserver_1 bash` не работает, проверьте точное имя:

```bash
# Посмотреть все контейнеры
docker ps --format "table {{.ID}}\t{{.Names}}\t{{.Status}}"

# Пример вывода:
# CONTAINER ID   NAMES                     STATUS
# abc123def456   yugabyte_yb-tserver_1     Up 5 minutes
# def456ghi789   yugabyte_yb-master_1      Up 5 minutes
```

И используйте имя из колонки `NAMES`.

### ✅ Что проверить после подключения

В `psql` или `ysqlsh` выполните:

```sql
-- Проверка версии
SELECT version();

-- Создание тестовой таблицы
CREATE TABLE IF NOT EXISTS test_table (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Вставка данных
INSERT INTO test_table (name) VALUES ('Данные из Docker Compose');

-- Чтение данных
SELECT * FROM test_table;
```

### 🎯 Итог

**Для ежедневной работы с БД** используйте `psql -h localhost -p 5433 -U yugabyte` — это самый удобный способ. Входить в контейнер нужно в основном для администрирования.
