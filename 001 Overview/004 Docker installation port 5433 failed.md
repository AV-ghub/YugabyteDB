Проблема в том, что контейнер либо не запущен, либо YSQL сервис внутри него не стартовал. Давайте диагностируем по шагам.

### 🔍 Диагностика и решение

**Шаг 1: Проверьте состояние контейнера**

```bash
docker ps -a | grep yugabyte-single
```

Вам важны два столбца: **STATUS** (должен быть "Up") и **PORTS** (должны отображаться проброшенные порты).

Возможные сценарии и решения:

| Если STATUS показывает... | Что делать |
| :--- | :--- |
| **`Exited`** (остановлен) | Контейнер завершил работу. Смотрим логи: `docker logs yugabyte-single`. Частая причина — ошибка выделения памяти или конфликт портов. |
| **`Up`** (работает) | Переходим к Шагу 2. |
| **Контейнер отсутствует** в списке | Значит, он не был создан. Запустите команду `docker run` из предыдущего шага заново. |

**Шаг 2: Если контейнер `Up`, смотрим логи на предмет ошибок запуска YSQL**

```bash
docker logs yugabyte-single | grep -A5 -B5 "YSQL\|ERROR\|FATAL"
```

**Шаг 3: Попробуйте подключиться ВНУТРИ контейнера (самый надежный тест)**

Если контейнер работает, но подключение с хоста падает, проблема может быть в сетевых настройках. Давайте проверим работу YSQL изнутри:

```bash
# 1. Заходим внутрь контейнера
docker exec -it yugabyte-single bash

# 2. Внутри контейнера подключаемся через ysqlsh
ysqlsh -U yugabyte

# 3. Выполняем тестовую команду
SELECT version();
```
Если этот способ сработает, значит YSQL работает, но есть проблема с пробросом порта `5433` с контейнера на хост.

**Шаг 4: Перезапустим контейнер с полным выводом логов (рекомендуемый способ)**

Давайте остановим, удалим старый контейнер и запустим заново, но **без флага `-d`**, чтобы видеть логи в реальном времени и поймать ошибку:

```bash
# 1. Останавливаем и удаляем старый контейнер
docker rm -f yugabyte-single

# 2. Запускаем БЕЗ -d, чтобы видеть логи
docker run \
  --name yugabyte-single \
  -p 5433:5433 \
  -p 7000:7000 \
  -p 9001:9000 \
  yugabytedb/yugabyte:latest \
  bin/yugabyted start \
  --daemon=false \
  --ui=false
```

**Не закрывайте это окно терминала!** В нем будут видны все логи запуска. Дождитесь сообщений:
- `YSQL listening on 0.0.0.0:5433`
- `Web server started on`
- `Completed starting up all services`

Это может занять 30-60 секунд. Как только увидите эти строки, YSQL готов. **Только после этого** открывайте новое окно терминала и пробуйте подключиться снова:

```bash
docker exec -it yugabyte-single bin/ysqlsh -U yugabyte
```

### 📊 Наиболее частые причины ошибки:

1.  **Недостаточно памяти** (самая частая): YugabyteDB по умолчанию требует ~1 ГБ RAM. Добавьте флаг `--memory=1024m` (или больше) в команду `docker run`.
2.  **Ошибка проброса порта**: Убедитесь, что порты `5433`, `7000` на хосте свободны (`sudo lsof -i :5433`).
3.  **Проблемы с файловой системой в контейнере**: Попробуйте удалить старый контейнер и образ и скачать заново.
