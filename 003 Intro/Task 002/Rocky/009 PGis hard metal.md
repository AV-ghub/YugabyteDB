```
нет, образов таких нет
 # Ищем теги с "debian" или "bullseye"
curl -s https://registry.hub.docker.com/v2/repositories/yugabytedb/yugabyte/tags | jq -r '.results[].name' | grep -i "debian\|bullseye\|bookworm" | head -10
[]#

а варианта поставить yugabyte прямо на железную машину нет?
может быть на хосте быстрее удастся что-то сделать?
```

Вы абсолютно правы. Если готовых Debian-образов YugabyteDB нет, а все Docker-пути ведут к проблемам с репозиториями или GLIBC, то **установка YugabyteDB напрямую на хост-машину (bare metal) — это отличная идея и, вероятно, самый быстрый путь к цели**.

На чистом хосте (вашей Rocky Linux 9.6) у вас будет полный контроль над системой: работающие репозитории, возможность установки любых пакетов и отсутствие изоляции Docker, которая вызывала большинство наших проблем.

### 🛠️ План: Установка YugabyteDB и PostGIS на Rocky Linux 9

Давайте разобьём процесс на чёткие шаги. Предполагается, что у вас есть доступ `sudo`.

#### Шаг 1: Установка YugabyteDB через официальный репозиторий
Это самый надёжный способ, рекомендованный в документации.

```bash
# 1. Добавляем репозиторий YugabyteDB
sudo tee /etc/yum.repos.d/yugabyte.repo << 'EOF'
[yugabyte]
name=YugabyteDB
baseurl=https://releases.yugabyte.com/releases/latest/rocky/9/x86_64
enabled=1
gpgcheck=1
gpgkey=https://releases.yugabyte.com/releases/latest/rocky/9/x86_64/repodata/repomd.xml.key
EOF

# 2. Устанавливаем YugabyteDB
sudo dnf install -y yugabyte

# 3. Инициализируем и запускаем одноузловой кластер
sudo /usr/local/yugabyte/bin/yugabyted start --base_dir=/home/yugabyte/yb_data --daemon=false --listen=0.0.0.0
```

#### Шаг 2: Установка PostgreSQL 15 и PostGIS
Теперь установим нужные версии PostGIS из стандартных репозиториев Rocky Linux/EPEL.

```bash
# 1. Устанавливаем репозитории PostgreSQL и EPEL
sudo dnf install -y epel-release
sudo dnf install -y https://download.postgresql.org/pub/repos/yum/reporpms/EL-9-x86_64/pgdg-redhat-repo-latest.noarch.rpm

# 2. Устанавливаем PostgreSQL 15 и PostGIS 3.5 (самая свежая для pg15 в репозиториях)
sudo dnf install -y postgresql15-server postgis35_15 postgis35_15-client

# 3. Инициализируем БД PostgreSQL (обычный postgres, не Yugabyte)
sudo /usr/pgsql-15/bin/postgresql-15-setup initdb
sudo systemctl start postgresql-15
```

#### Шаг 3: Ключевой шаг: Копирование файлов PostGIS в YugabyteDB
Это сердце процесса. Мы берём файлы из только что установленной системы и помещаем их в каталоги запущенного кластера YugabyteDB.

```bash
# 1. Останавливаем YugabyteDB (чтобы безопасно копировать файлы)
sudo /usr/local/yugabyte/bin/yugabyted stop --base_dir=/home/yugabyte/yb_data

# 2. Копируем библиотеки (.so) и файлы расширений (.sql, .control) PostGIS
#    из системных каталогов PostgreSQL в каталоги YugabyteDB
sudo cp -r /usr/pgsql-15/lib/postgis* /usr/local/yugabyte/postgres/lib/
sudo cp -r /usr/share/pgsql-15/extension/postgis* /usr/local/yugabyte/postgres/share/extension/

# 3. Запускаем YugabyteDB заново
sudo /usr/local/yugabyte/bin/yugabyted start --base_dir=/home/yugabyte/yb_data --daemon=false --listen=0.0.0.0

# 4. Даём кластеру время на запуск (30-60 сек)
sleep 60
```

#### Шаг 4: Проверка работы PostGIS в YugabyteDB
Подключаемся к YSQL-порту YugabyteDB (по умолчанию `5433`) и проверяем.

```bash
# Подключаемся с помощью клиента, который шёл в комплекте с YugabyteDB
/usr/local/yugabyte/bin/ysqlsh -h localhost -p 5433 -U yugabyte << 'EOF'
CREATE EXTENSION postgis;
SELECT PostGIS_Full_Version();
EOF
```

### 💡 Преимущества этого подхода
1.  **Решает проблему GLIBC**: Библиотеки PostGIS собираются **непосредственно в вашей системе** (Rocky Linux 9) и гарантированно совместимы с её GLIBC.
2.  **Обходит проблемы Docker**: Нет конфликтов сетей, репозиториев или изоляции.
3.  **Прямой доступ и контроль**: Все файлы на виду, их легко копировать и диагностировать.
4.  **Скорость**: Установка из родных репозиториев происходит быстро.

### ⚠️ Важные замечания
*   **Два сервера PostgreSQL**: На хосте будут работать **два разных сервера**:
    1.  Обычный PostgreSQL 15 (порт по умолчанию `5432`) — только как источник файлов PostGIS.
    2.  YugabyteDB (порт `5433`) — наша основная цель.
*   **Порты**: Убедитесь, что порты `5432` (для шага 2) и `5433` (для YugabyteDB) свободны.
*   **Версии**: В командах используется `postgis35_15` для PostgreSQL 15. Если при установке возникнет ошибка `package not found`, проверьте доступные пакеты командой `dnf search postgis`. Возможно, вам подойдёт `postgis34_15` или `postgis33_15`.

### 🚀 Начинайте со Шага 1
Этот путь технически проще всего, что мы пробовали, и имеет высокие шансы на успех.

**Начните со Шага 1 (установка YugabyteDB).** Если на каком-то этапе возникнут трудности (например, репозиторий не найдётся или пакет PostGIS будет иметь другое имя), сразу пишите — скорректируем команды под ваше окружение.
