```
процесс идет конечно, но чувствую что начинаю уставать
конца этому не видно
алгоритм простой
пускаем
docker exec -it yugabyte-pg /home/yugabyte/bin/ysqlsh -h 172.16.0.2 -p 5433 -U yugabyte -c "CREATE EXTENSION postgis; SELECT PostGIS_Full_Version();"
получаем ошибку вида
ERROR:  could not load library "/home/yugabyte/postgres/lib/postgis-3.so": libssh2.so.1: cannot open shared object file: No such file or directory
берем оттуда например libssh2.so.1
берем шаблон
docker run --rm postgis/postgis:15-3.5 bash -c 'ls -la /usr/lib/x86_64-linux-gnu/0000000*'
куда вместо ноликов подставляем то что получили (libssh2.so.1)
получаем что-то типа
lrwxrwxrwx 1 root root     16 Nov 29  2023 /usr/lib/x86_64-linux-gnu/libssh2.so.1 -> libssh2.so.1.0.1
-rw-r--r-- 1 root root 211272 Nov 29  2023 /usr/lib/x86_64-linux-gnu/libssh2.so.1.0.1
или без первой строки, без симлинка
потом берем следующий шаблон
docker cp temp_postgis:/usr/lib/x86_64-linux-gnu/0000000 /tmp/
docker cp temp_postgis:/usr/lib/x86_64-linux-gnu/1111111 /tmp/
docker cp /tmp/1111111 yugabyte-pg:/home/yugabyte/lib/yb-thirdparty/
docker cp /tmp/0000000 yugabyte-pg:/home/yugabyte/lib/yb-thirdparty/
docker exec yugabyte-pg bash -c "cd /home/yugabyte/lib/yb-thirdparty/ && ln -sf 0000000 1111111"
docker exec yugabyte-pg ldconfig /home/yugabyte/lib/yb-thirdparty/
docker exec -it yugabyte-pg /home/yugabyte/bin/ysqlsh -h 172.16.0.2 -p 5433 -U yugabyte -c "CREATE EXTENSION postgis; SELECT PostGIS_Full_Version();"
расставляем туда соотв файлик и симлинк
пускаем, получаем следующую ошибку и далее по кругу
напрашивается мысль о написании скрипта
можно такое сделать?
или может есть какой-то еще обходной вариант?
библиотек очень много, чувствую тут день уйдет, и может быть не один
```

Вы абсолютно правы! Этот процесс бесконечный и изматывающий. Добавление каждой библиотеки вручную может занять целый день, потому что PostGIS имеет **десятки зависимостей**. Необходим **скрипт или принципиально другой подход**.

### ⚡ Решение 1: Автоматический скрипт для копирования ВСЕХ зависимостей
Давайте найдём и скопируем **все зависимости библиотеки `postgis-3.so`** сразу. Этот скрипт выполнит всю рутинную работу за нас.

Создайте на хост-машине файл `copy_deps.sh`:

```bash
cat > copy_deps.sh << 'EOF'
#!/bin/bash
# 1. Создаем временный контейнер с PostGIS
CONTAINER_ID=$(docker create postgis/postgis:15-3.5)

# 2. Находим путь к библиотеке postgis-3.so внутри образа
POSTGIS_LIB_PATH=$(docker run --rm postgis/postgis:15-3.5 find /usr -name "postgis-3.so" 2>/dev/null | head -1)
echo "Найдена библиотека: $POSTGIS_LIB_PATH"

# 3. Используем ldd внутри контейнера, чтобы получить СПИСОК ВСЕХ зависимостей
DEPS=$(docker run --rm postgis/postgis:15-3.5 ldd $POSTGIS_LIB_PATH | grep '=> /' | awk '{print $3}')

# 4. Для каждой зависимости копируем её (и все версии/симлинки) в контейнер YugabyteDB
for DEP in $DEPS; do
    echo "Копирую: $DEP"
    # Находим все файлы с таким именем (основной файл и симлинки)
    FILES=$(docker run --rm postgis/postgis:15-3.5 find /usr -name "$(basename $DEP)*" 2>/dev/null)
    for FILE in $FILES; do
        echo "  -> $FILE"
        docker cp $CONTAINER_ID:$FILE /tmp/ 2>/dev/null
        docker cp /tmp/$(basename $FILE) yugabyte-pg:/home/yugabyte/lib/yb-thirdparty/ 2>/dev/null
        rm -f /tmp/$(basename $FILE)
    done
done

# 5. Очищаем временный контейнер
docker rm $CONTAINER_ID >/dev/null

# 6. Обновляем кэш библиотек в контейнере YugabyteDB
docker exec yugabyte-pg ldconfig /home/yugabyte/lib/yb-thirdparty/

echo "Готово! Все зависимости скопированы."
EOF

# Даем права на выполнение
chmod +x copy_deps.sh
```

Запустите скрипт:
```bash
./copy_deps.sh
```

После этого проверьте PostGIS:
```bash
docker exec -it yugabyte-pg /home/yugabyte/bin/ysqlsh -h 172.16.0.2 -p 5433 -U yugabyte -c "CREATE EXTENSION postgis; SELECT PostGIS_Full_Version();"
```

### 🔧 Решение 2: Создание идеального Dockerfile (рекомендуется)
Пока скрипт работает, создайте **окончательный Dockerfile**, который решает проблему раз и навсегда. Он использует `ldd` для автоматического поиска и копирования всех зависимостей:

```dockerfile
# Этап 1: Берем официальный образ PostGIS
FROM postgis/postgis:15-3.5 AS postgis_source

# Находим все зависимости postgis-3.so и упаковываем их в архив
RUN POSTGIS_LIB=$(find /usr -name "postgis-3.so" | head -1) && \
    ldd $POSTGIS_LIB | grep '=> /' | awk '{print $3}' | xargs -I {} find /usr -name "$(basename {})*" 2>/dev/null | \
    tar -czf /tmp/all-postgis-deps.tar.gz -T -

# Этап 2: Финальный образ YugabyteDB
FROM yugabytedb/yugabyte:2025.2.0.1-b1

# Копируем основные файлы PostGIS
COPY --from=postgis_source /usr/lib/postgresql/15/lib/postgis*.so /home/yugabyte/postgres/lib/
COPY --from=postgis_source /usr/share/postgresql/15/extension/postgis* /home/yugabyte/postgres/share/extension/

# Копируем и распаковываем ВСЕ зависимости
COPY --from=postgis_source /tmp/all-postgis-deps.tar.gz /tmp/
RUN tar -xzf /tmp/all-postgis-deps.tar.gz -C /home/yugabyte/lib/yb-thirdparty/ && \
    rm -f /tmp/all-postgis-deps.tar.gz

# Перелинковка и обновление кэша
RUN /home/yugabyte/bin/post_install.sh -e && \
    ldconfig /home/yugabyte/lib/yb-thirdparty/
```

**Соберите этот образ — он должен решить все проблемы с зависимостями разом.**

### 🎯 Почему это сработает
1.  **`ldd postgis-3.so`** показывает **все динамические зависимости** библиотеки.
2.  **`find -name "$(basename {})*"`** находит не только саму библиотеку, но и все её симлинки.
3.  **`tar -czf -T -`** упаковывает весь найденный список файлов, сохраняя структуру симлинков.

### 📊 Сравнение подходов

| Подход | Плюсы | Минусы | Когда использовать |
|--------|-------|--------|-------------------|
| **Скрипт `copy_deps.sh`** | Быстрая проверка, не требует пересборки | Временное решение | Сейчас, чтобы сразу протестировать |
| **Идеальный Dockerfile** | Постоянное решение, воспроизводимость | Нужно пересобирать образ | Для финальной версии образа |
| **Ручное копирование** | Контроль над каждым шагом | Бесконечно долго | Только для отладки конкретных проблем |

### 💡 Рекомендация
1.  **Запустите скрипт `copy_deps.sh`** — это даст быстрый результат.
2.  **Если сработает** — создайте и соберите "идеальный Dockerfile" для фиксации решения.
3.  **Если останется 1-2 библиотеки** — добавьте их вручную в Dockerfile.

**Попробуйте скрипт.** Он должен автоматически скопировать 20-30 библиотек за несколько секунд. Если после его выполнения останутся ошибки — пришлите их, и мы дополним решение.


-----------------------------------------
-------------------------------------
-----------------------------------------


```
скрипт великолепно отработал, но что-то еще не нашлось
docker exec -it yugabyte-pg /home/yugabyte/bin/ysqlsh -h 172.16.0.2 -p 5433 -U yugabyte -c "CREATE EXTENSION postgis; SELECT PostGIS_Full_Version();"
ERROR:  could not load library "/home/yugabyte/postgres/lib/postgis-3.so": /lib64/libm.so.6: version `GLIBC_2.29' not found (required by /home/yugabyte/postgres/lib/postgis-3.so)

можем это поправить? не вручную конечно)
```

Это **последняя и самая серьёзная ошибка** — конфликт версий **GLIBC** (GNU C Library). Библиотека PostGIS, собранная для современного дистрибутива (Ubuntu/Debian в образе `postgis/postgis`), требует более новой версии GLIBC (`2.29`), чем та, что есть в образе YugabyteDB, основанном на AlmaLinux (`2.28` или ниже).

Это **архитектурное ограничение**, которое нельзя решить простым копированием библиотек. Есть три пути, расположенные в порядке предпочтительности:

### 🎯 Путь 1: Использовать образ YugabyteDB на Debian/Ubuntu (самый правильный)
YugabyteDB официально предоставляет образы на основе **Debian**, которые совместимы с `postgis/postgis`. Это решит проблему раз и навсегда.

1.  **Найдите нужный тег** на [Docker Hub YugabyteDB](https://hub.docker.com/r/yugabytedb/yugabyte/tags). Ищите теги с суффиксом `-debian` или `-bionic`/`-focal`. Например, `2025.2.0.1-b1-debian`.
2.  **Измените базовый образ** в вашем `Dockerfile`:
    ```dockerfile
    # Вторая строка (FROM для финального образа)
    FROM yugabytedb/yugabyte:2025.2.0.1-b1-debian  # Используем Debian-образ
    ```
3.  **Пересоберите образ** — все зависимости PostGIS теперь будут полностью совместимы.

### 🔧 Путь 2: Собрать PostGIS из исходников в среде YugabyteDB (сложно, но надёжно)
Если образа на Debian нет, можно скомпилировать PostGIS внутри контейнера YugabyteDB, что гарантирует совместимость библиотек.

Вот упрощённый шаблон для Dockerfile:
```dockerfile
FROM yugabytedb/yugabyte:2025.2.0.1-b1

# Устанавливаем инструменты для сборки
RUN yum install -y gcc make postgresql15-devel proj-devel geos-devel gdal-devel

# Скачиваем и распаковываем исходники PostGIS
WORKDIR /tmp
RUN curl -L https://download.osgeo.org/postgis/source/postgis-3.5.2.tar.gz | tar -xz

# Компилируем и устанавливаем PostGIS
WORKDIR /tmp/postgis-3.5.2
RUN ./configure --with-pgconfig=/home/yugabyte/postgres/bin/pg_config
RUN make
RUN make install

# Копируем файлы в нужные каталоги YugabyteDB
RUN cp -r /usr/local/lib/postgresql/postgis* /home/yugabyte/postgres/lib/
RUN cp -r /usr/local/share/postgresql/extension/postgis* /home/yugabyte/postgres/share/extension/

RUN /home/yugabyte/bin/post_install.sh -e
```

**Недостаток:** Сборка занимает 15-30 минут и требует точного подбора версий зависимостей.

### ⚠️ Путь 3: Обходной путь с понижением версии PostGIS (рискованный)
Можно попытаться найти более старую версию образа `postgis/postgis`, собранную со старой GLIBC. Однако это временное и ненадёжное решение.

```dockerfile
# Попробуйте более старый тег, например, для PostgreSQL 14
FROM postgis/postgis:14-3.3 AS postgis_source
```

### 💡 Рекомендация и итог
**Настоятельно рекомендую **Путь 1**. Если официального Debian-образа YugabyteDB под вашу версию нет, можно использовать тег `latest-debian`.

| Путь | Сложность | Надёжность | Время |
|------|-----------|------------|-------|
| **1. Debian-образ YugabyteDB** | Низкая | ⭐⭐⭐⭐⭐ (идеально) | 10 мин |
| **2. Сборка из исходников** | Высокая | ⭐⭐⭐⭐⭐ (надёжно) | 30-60 мин |
| **3. Старый PostGIS** | Средняя | ⭐⭐ (риск несовместимости) | 15 мин |

**Ваши действия:**
1.  Проверьте наличие тега `-debian` для вашей версии YugabyteDB.
2.  Если есть — измените `Dockerfile` и пересоберите образ.
3.  Если нет — придётся идти Путем 2 (сборка из исходников).

Попробуйте найти Debian-образ. Если не получится — сообщите, и я помогу с Dockerfile для сборки PostGIS из исходников.
