🎉 **Отлично!** Да, теперь у вас есть стабильная основа — одноузловой YugabyteDB, работающий прямо на хосте. Это **идеальная позиция** для финального шага — установки PostGIS.

Возвращаемся к плану, адаптированному под локальную установку. Процесс теперь должен пройти гораздо глаже, так как:
1.  У вас есть полный контроль над системой (работающие репозитории `dnf`).
2.  Нет конфликтов сетей Docker и версий GLIBC.
3.  Все файлы находятся на одном хосте.

### 🛠️ План установки PostGIS в локальный YugabyteDB

Выполните шаги последовательно. Предполагаем, что YugabyteDB установлен в директорию `~/yugabyte-2025.2.0.1` и работает.

#### Шаг 1: Установка PostgreSQL 15 и PostGIS из репозиториев Rocky Linux

```bash
# 1. Убедитесь, что репозитории PostgreSQL и EPEL добавлены (возможно, вы уже это делали)
sudo dnf install -y epel-release
sudo dnf install -y https://download.postgresql.org/pub/repos/yum/reporpms/EL-9-x86_64/pgdg-redhat-repo-latest.noarch.rpm

# 2. Установите клиентские библиотеки PostgreSQL 15 и PostGIS 3.5
#    Нам не нужен сервер PostgreSQL, только файлы (библиотеки и расширения).
sudo dnf install -y postgresql15 postgis35_15
```

#### Шаг 2: Копирование файлов PostGIS в YugabyteDB

**Важно:** Перед копированием нужно **остановить** YugabyteDB.

```bash
# 1. Определите путь к вашей установке YugabyteDB (замените на ваш реальный путь)
YUGABYTE_DIR=~/yugabyte-2025.2.0.1

# 2. Остановите YugabyteDB
$YUGABYTE_DIR/bin/yugabyted stop

# 3. Скопируйте библиотеки (.so) и файлы расширений (.sql, .control) PostGIS
sudo cp -r /usr/pgsql-15/lib/postgis* $YUGABYTE_DIR/postgres/lib/
sudo cp -r /usr/share/pgsql-15/extension/postgis* $YUGABYTE_DIR/postgres/share/extension/

# 4. Запустите YugabyteDB заново
$YUGABYTE_DIR/bin/yugabyted start --background=false
sleep 60  # Дайте время на полный запуск
```

#### Шаг 3: Проверка работы PostGIS

```bash
# Подключитесь к YSQL и создайте расширение
$YUGABYTE_DIR/bin/ysqlsh -h localhost -p 5433 -U yugabyte << 'EOF'
CREATE EXTENSION postgis;
SELECT PostGIS_Full_Version();
EOF
```

### ⚠️ Возможные проблемы и их решение

1.  **Ошибка: `libgeos_c.so.1: cannot open shared object file`**
    Это означает, что YugabyteDB не нашёл системные библиотеки PostGIS. Их нужно скопировать в его каталог со сторонними библиотеками.
    ```bash
    # Остановите YugabyteDB и скопируйте зависимости GEOS, PROJ, GDAL
    $YUGABYTE_DIR/bin/yugabyted stop
    sudo cp /usr/lib64/libgeos_c.so.1 $YUGABYTE_DIR/lib/yb-thirdparty/
    sudo cp /usr/lib64/libproj.so.25 $YUGABYTE_DIR/lib/yb-thirdparty/
    sudo cp /usr/lib64/libgdal.so.32 $YUGABYTE_DIR/lib/yb-thirdparty/
    # Запустите снова и проверьте
    $YUGABYTE_DIR/bin/yugabyted start --background=false
    ```

2.  **Пакет `postgis35_15` не найден**
    Проверьте доступные версии: `sudo dnf search postgis`. Возможно, вам нужен `postgis34_15` или `postgis33_15`. Установите подходящий.

3.  **Ошибка прав доступа при копировании**
    Если команда `sudo cp` не срабатывает, убедитесь, что у вас есть права на запись в целевую директорию. Можно скопировать от пользователя:
    ```bash
    cp -r /usr/pgsql-15/lib/postgis* $YUGABYTE_DIR/postgres/lib/
    ```

### 📝 Что дальше?
После успешной проверки (`SELECT PostGIS_Full_Version();` вернул результат) вы можете:
*   **Протестировать базовые функции** PostGIS (создание точек, расчёт расстояний).
*   **Настроить кластер для production** (добавить узлы, настроить репликацию).
*   **Перенести конфигурацию в Docker**, используя успешный опыт локальной установки для создания финального Dockerfile.

**Начните со Шага 1.** Если на этапе установки пакетов или копирования возникнут ошибки — пришлите точное сообщение, и мы найдём решение. Теперь, когда основа стабильна, шансы на успех близки к 100%.

------------------------
----------------
--------------------

```
снова проблема
yum install postgis35_15
Repository epel is listed more than once in the configuration
YugabyteDB                                                                                                                                                                    132  B/s | 255  B     00:01
Errors during downloading metadata for repository 'yugabyte':
  - Status code: 403 for https://releases.yugabyte.com/releases/latest/rocky/9/x86_64/repodata/repomd.xml (IP: 8.6.112.0)
Error: Failed to download metadata for repo 'yugabyte': Cannot download repomd.xml: Cannot download repodata/repomd.xml: All mirrors were tried
```
Это **не проблема установки**, а всего лишь **ошибка в одном из репозиториев** (`yugabyte`), который мы добавляли ранее для установки через `yum`. Она **не помешает** установить PostGIS из основных репозиториев (Rocky Linux, EPEL, PGDG).

Ошибка возникает потому, что репозиторий YugabyteDB недоступен (возвращает 403), но `yum` всё равно пытается проверить его метаданные при каждом вызове. Эту проблему можно **проигнорировать** или **временно отключить** репозиторий.

### 🚀 Быстрое решение: Установите PostGIS, игнорируя ошибку

Просто выполните команду с дополнительным флагом, чтобы `yum` не обращал внимания на сбойный репозиторий:

```bash
sudo yum install postgis35_15 --disablerepo=yugabyte
```

Флаг `--disablerepo=yugabyte` говорит системе временно не использовать проблемный репозиторий для этой операции. Установка пойдёт из остальных рабочих репозиториев (PGDG, EPEL).

**Внимательно посмотрите на вывод команды.** Вас интересует список пакетов в разделе **"Installing:"**. Если там есть `postgis35_15` и зависимости (например, `postgresql15-libs`, `geos35`, `proj85`), а в разделе **"Removing:"** или **"Replacing:"** **нет** пакетов с `postgresql14`, то можно смело подтверждать установку (`y`).

### ⚙️ Альтернатива: Постоянно отключите сбойный репозиторий

Если хотите избавиться от этого сообщения навсегда, отключите репозиторий:

```bash
sudo yum-config-manager --disable yugabyte
# Затем установите PostGIS как обычно
sudo yum install postgis35_15
```

### 💡 Что делать, если пакет `postgis35_15` не найден?

Иногда в репозиториях для определённой версии Rocky Linux может быть другая версия. Проверьте, какие версии PostGIS для PostgreSQL 15 доступны:

```bash
sudo yum search postgis | grep 15
```

Если `postgis35_15` нет, возможно, есть `postgis34_15` или `postgis33_15`. Установите ту версию, которая есть.

### 🛠️ Если установка прошла успешно
После установки продолжаем по плану:
1.  Остановите YugabyteDB.
2.  Скопируйте файлы из `/usr/pgsql-15/lib/postgis*` и `/usr/share/pgsql-15/extension/postgis*`.
3.  Запустите YugabyteDB и проверьте расширение.

**Попробуйте команду с `--disablerepo=yugabyte` и сообщите, что показал `yum` в списке устанавливаемых/удаляемых пакетов.**
