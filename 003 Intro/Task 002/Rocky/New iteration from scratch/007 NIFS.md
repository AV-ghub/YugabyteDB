В контейнере есть проблемы с сертификатами. У вас в итоге два пути, и оба они ведут к нашей главной цели.

### 🔍 **Кратко: почему это происходит**
Ошибка указывает на проблему с SSL-сертификатами, которая мешает `dnf` подключиться к репозиториям. В вашем минимальном контейнере, скорее всего, либо неактуальный набор доверенных сертификатов (CA), либо используется очень строгая политика шифрования (например, `FUTURE` или `FIPS`), которая не принимает сертификаты зеркал AlmaLinux.

### ✅ **Вариант 1: Исправить работу `dnf` в контейнере (быстрее, если сработает)**
Попробуйте выполнить следующие команды внутри контейнера:

```bash
# 1. Проверьте текущую политику шифрования. Часто помогает сменить её на DEFAULT.
update-crypto-policies --show
update-crypto-policies --set DEFAULT
# После этого иногда нужен перезапуск, но в контейнере можно просто продолжить.
```

Если не поможет, попробуйте обновить набор доверенных сертификатов (мог устареть в образе 2025 года):
```bash
# 2. Обновить и извлечь доверенные корневые сертификаты.
update-ca-trust
update-ca-trust extract
```

После этих шагов попробуйте снова:
```bash
dnf install -y wget gcc make
```
Если `dnf` начнёт работать, вы сможете продолжить по первоначальному плану.

### ✅ **Вариант 2: Сборка на виртуальной машине с CentOS 7 (надёжнее)**
Если с контейнером возникнут сложности, лучше вернуться к этому плану. Вот как его безопасно выполнить:

#### Шаг 1: Подготовить файлы YugabyteDB на виртуалке
На вашей основной машине создайте архив с нужными файлами для сборки и скопируйте его на виртуалку:
```bash
# На вашей основной Rockey Linux (вне контейнера)
tar -czf yb-for-build.tar.gz \
    /_a/yugabyte-2025.2.0.1/postgres/bin/pg_config \
    /_a/yugabyte-2025.2.0.1/postgres/include/
```
Перенесите файл `yb-for-build.tar.gz` на виртуальную машину с CentOS 7.

#### Шаг 2: На виртуалке установить зависимости и собрать PostGIS
Войдите на виртуальную машину с CentOS 7 и выполните:
```bash
# 1. Установите компилятор и базовые зависимости PostGIS
sudo yum install -y gcc make postgresql-devel geos-devel proj-devel gdal-devel libxml2-devel

# 2. Распакуйте архив с файлами YugabyteDB
tar -xzf yb-for-build.tar.gz -C /tmp/

# 3. Скачайте и соберите PostGIS
cd /tmp
wget https://download.osgeo.org/postgis/source/postgis-3.3.8.tar.gz
tar -xzf postgis-3.3.8.tar.gz
cd postgis-3.3.8

# Ключевой момент: используем pg_config из вашего YugabyteDB
./configure --with-pgconfig=/tmp/_a/yugabyte-2025.2.0.1/postgres/bin/pg_config
make
```

#### Шаг 3: Перенести готовые файлы на основную машину
После успешной сборки файлы расширения будут в каталоге сборки. Самые важные:
- `.so`-файлы (библиотеки) в подкаталоге `lib/`
- `.sql` и `.control` файлы в подкаталоге `extension/`
Скопируйте их (например, через `scp`) обратно на вашу основную машину в соответствующие каталоги YugabyteDB.

### 🧭 **Какой вариант выбрать?**
| Подход | Плюсы | Минусы | Когда выбирать |
| :--- | :--- | :--- | :--- |
| **Исправить контейнер** | Быстрее, если сработает. Окружение максимально совместимое. | Есть риск, что проблема с сетью/сертификатами не решится просто. | Если вы хотите быстро проверить, сработает ли обходной путь из статьи. |
| **Сборка на CentOS 7** | Надёжнее. Все зависимости точно установятся из работающих репозиториев. | Нужно копировать файлы туда-обратно, процесс чуть дольше. | Если нужен гарантированный результат без лишней борьбы с контейнером. |

**Рекомендация**: Сначала попробуйте **Вариант 1** в контейнере. Если после смены политики на `DEFAULT` команда `dnf install -y wget` не сработает в течение 5-10 минут — переключайтесь на **Вариант 2** с виртуалкой.

Попробуйте исправить контейнер и напишите, получилось ли установить `wget` и `gcc`?
