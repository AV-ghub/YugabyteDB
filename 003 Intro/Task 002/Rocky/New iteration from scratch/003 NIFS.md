Отлично! Вы нашли ключевую информацию. 

**Вывод:** Ваш YugabyteDB собран с GLIBC до версии **2.25**. Самая новая требуемая версия — `GLIBC_2.25`. Это значит, что образы **AlmaLinux 8** (которые используют glibc 2.28) из статьи нам **не подходят** — они слишком новые.

Но у нас есть идеальное решение: **вы нашли Docker-образ для почти точной версии: `2025.2.0.1-b1`**. Это лучшая среда для сборки, так как внутри неё YugabyteDB собран с точно такими же системными библиотеками, как и у вас.

### 📋 Обновлённый план действий (теперь конкретный)

Мы будем следовать адаптированной логике статьи, но используем **правильный, совместимый образ**.

#### Шаг 1: Скачаем и изучим образ
```bash
docker pull yugabytedb/yugabyte:2025.2.0.1-b1-x86_64
```

#### Шаг 2: Запустим контейнер и войдём в него
```bash
# Запускаем контейнер в интерактивном режиме, монтируем каталог для удобного копирования файлов
docker run -it --name yb-build -v /tmp/postgis_build:/mnt yugabytedb/yugabyte:2025.2.0.1-b1-x86_64 /bin/bash
```

#### Шаг 3: Внутри контейнера установим всё для сборки PostGIS
После входа в контейнер (`#` — это приглашение внутри контейнера) выполните:
```bash
# 1. Переключаемся на root (в контейнере обычно уже root, но на всякий случай)
sudo su - || whoami

# 2. Обновляем пакетный менеджер (используем microdnf, т.к. это, скорее всего, образ на базе UBI/Alma)
microdnf update -y

# 3. Устанавливаем инструменты сборки и зависимости PostGIS
#    ВАЖНО: Устанавливаем postgresql15-devel (совпадает с версией PG в YB)
microdnf install -y gcc make postgresql15-devel geos-devel proj-devel gdal-devel libxml2-devel git
```

#### Шаг 4: Скачиваем и собираем PostGIS
```bash
# 1. Выбираем версию PostGIS. Для PG15 лучше взять 3.3.х. Например, 3.3.8
cd /tmp
wget https://download.osgeo.org/postgis/source/postgis-3.3.8.tar.gz
tar -xzf postgis-3.3.8.tar.gz
cd postgis-3.3.8

# 2. Конфигурируем, используя pg_config из контейнера
./configure --with-pgconfig=/usr/pgsql-15/bin/pg_config

# 3. Собираем и "устанавливаем" во временный каталог (не в систему контейнера)
make
make DESTDIR=/mnt/postgis_install install
```
После этого в `/mnt/postgis_install` (а значит, и в `/tmp/postgis_build` на вашей хостовой машине) появятся собранные файлы.

#### Шаг 5: Копируем файлы в локальную установку YugabyteDB
Выйдите из контейнера (`exit`) и на вашей хостовой машине (Rockey Linux) выполните:
```bash
# 1. Копируем библиотеку (.so)
sudo cp /tmp/postgis_build/postgis_install/usr/pgsql-15/lib/postgis*.so /_a/yugabyte-2025.2.0.1/postgres/lib/

# 2. Копируем SQL и control-файлы расширения
sudo cp /tmp/postgis_build/postgis_install/usr/pgsql-15/share/extension/postgis* /_a/yugabyte-2025.2.0.1/postgres/share/extension/

# 3. (Важно!) Устанавливаем владельца файлов, как у других файлов YB
sudo chown -R yugabyte:yugabyte /_a/yugabyte-2025.2.0.1/postgres/lib/postgis*.so
sudo chown -R yugabyte:yugabyte /_a/yugabyte-2025.2.0.1/postgres/share/extension/postgis*
```

#### Шаг 6: Проверяем
Перезапустите кластер YugabyteDB и попробуйте создать расширение:
```sql
CREATE EXTENSION postgis;
SELECT postgis_version();
```

### ⚠️ Что может пойти не так и запасной план
1.  **В образе другой менеджер пакетов.** Если `microdnf` не сработает, попробуйте `dnf` или `yum`. Логика та же.
2.  **Нет пакета `postgresql15-devel`.** Это самая вероятная проблема. В этом случае **используйте виртуалку с CentOS 7**.
    *   На виртуалку скопируйте папку `/_a/yugabyte-2025.2.0.1/postgres/include` и файл `/_a/yugabyte-2025.2.0.1/postgres/bin/pg_config`.
    *   Собирайте PostGIS на виртуалке, но укажите `configure` путь к вашему `pg_config`:
        ```bash
        ./configure --with-pgconfig=/path/to/copied/pg_config
        ```

### 🚀 Рекомендация к действию
Начните со **Шага 2 и 3**. Если внутри контейнера успешно установится `postgresql15-devel` — это 90% успеха. Если нет — переходим на запасной план с виртуалкой.

**Попробуйте зайти в контейнер и установить пакеты. Сообщите, что получилось.**
