```
configure: error: could not find libpq
```

Это известная проблема: `configure` пытается найти и линковаться с библиотекой `libpq`, которая является частью YugabyteDB, но из-за различий в путях или зависимостях это не удаётся.

### 🎯 Решение: Собрать PostGIS, обойдя проверку `libpq`

Вместо того чтобы пытаться заставить `configure` найти `libpq`, мы просто **обманем проверку** и явно укажем все пути. Мы уже делали нечто подобное для CentOS 7, но теперь все зависимости (`geos-config`, `gdal-config` и т.д.) уже установлены в системе.

Выполните эту команду в директории `/tmp/postgis-3.3.8`:

```bash
# 1. Очистите предыдущую конфигурацию
make clean

# 2. Запустите configure с полным набором параметров, обходящих проверку libpq
./configure \
  --without-pgconfig \
  --with-pgincdir=/_a/yugabyte-2025.2.0.1/postgres/include \
  --with-pglibdir=/_a/yugabyte-2025.2.0.1/postgres/lib \
  --with-geosconfig=/bin/geos-config \
  --with-projdir=/usr \
  --with-gdalconfig=/bin/gdal-config \
  --with-xml2config=/bin/xml2-config \
  LDFLAGS="-L/_a/yugabyte-2025.2.0.1/postgres/lib -Wl,-rpath,/_a/yugabyte-2025.2.0.1/postgres/lib" \
  CPPFLAGS="-I/_a/yugabyte-2025.2.0.1/postgres/include"
```

**Что это делает:**
- `--without-pgconfig` отключает попытку запустить `pg_config`.
- `--with-pgincdir` и `--with-pglibdir` указывают компилятору, где искать заголовочные файлы и библиотеки YugabyteDB.
- Остальные параметры (`--with-geosconfig` и т.д.) указывают на уже установленные в системе инструменты.
- `LDFLAGS` и `CPPFLAGS` гарантируют, что компоновщик и препроцессор будут искать библиотеки и заголовки в каталогах YugabyteDB.

### ✅ После успешного `configure`
Если `configure` завершится успешно (вы увидите сводную таблицу с версиями), выполните:
```bash
make
```
После успешной сборки остановите кластер, скопируйте новую библиотеку `postgis-3.so` и запустите кластер заново.

### 🔄 Если не сработает
В редких случаях может потребоваться явно указать библиотеку `libpq` во флагах линковки. Тогда попробуйте добавить `-lpq` в `LDFLAGS`:
```bash
LDFLAGS="-L/_a/yugabyte-2025.2.0.1/postgres/lib -Wl,-rpath,/_a/yugabyte-2025.2.0.1/postgres/lib -lpq"
```

**Попробуйте выполнить длинную команду `configure`. Сообщите, пройдёт ли она до конца.**

```
./configure \
  --without-pgconfig \
  --with-pgincdir=/_a/yugabyte-2025.2.0.1/postgres/include \
  --with-pglibdir=/_a/yugabyte-2025.2.0.1/postgres/lib \
  --with-geosconfig=/bin/geos-config \
  --with-projdir=/usr \
  --with-gdalconfig=/bin/gdal-config \
  --with-xml2config=/bin/xml2-config \
  LDFLAGS="-L/_a/yugabyte-2025.2.0.1/postgres/lib -Wl,-rpath,/_a/yugabyte-2025.2.0.1/postgres/lib" \
  CPPFLAGS="-I/_a/yugabyte-2025.2.0.1/postgres/include"
configure: WARNING: unrecognized options: --with-pgincdir, --with-pglibdir

configure: WARNING: xml2pot is not installed so translations cannot be updated
checking for msgmerge... /bin/msgmerge
checking for msginit... /bin/msginit
checking for convert... no
configure: WARNING: ImageMagick does not seem to be installed. Documentation cannot be built
checking for dblatex... no
configure: WARNING: dblatex is not installed so PDF documentation cannot be built
checking for xmllint... /bin/xmllint
configure: WARNING: could not locate Docbook stylesheets required to build the documentation
checking for pkg-config... /bin/pkg-config
checking pkg-config is at least version 0.9.0... yes
checking for CUNIT... no
configure: WARNING: could not locate CUnit required for unit tests

checking for libiconvctl... no
PostgreSQL support disabled
Using user-specified xml2-config file: /bin/xml2-config

configure: WARNING: "Could not find json-c"

ADDRESS_STANDARDIZER support: disabled
TOPOLOGY: Topology support requested
RASTER: Raster support requested
checking GDAL version... 3.10.3
checking for OGR enabled... yes
./configure: line 20465: no: command not found
./configure: line 20467: no: command not found

configure: WARNING: gdal.h: present but cannot be compiled
configure: WARNING: gdal.h:     check for missing prerequisite headers?
configure: WARNING: gdal.h: see the Autoconf documentation
configure: WARNING: gdal.h:     section "Present But Cannot Be Compiled"
configure: WARNING: gdal.h: proceeding with the compiler's result
checking for gdal.h... no
configure: error: could not find GDAL headers
```

Отлично, мы близки! Главное — **PostgreSQL support disabled** в логе означает, что `configure` принял наши флаги, но не понял нестандартные опции `--with-pgincdir` и `--with-pglibdir`. Это нормально. Ключевая ошибка теперь связана с **GDAL**: система нашла `gdal-config`, но не может скомпилировать проверочный код из-за отсутствия заголовков PostgreSQL.

Это классическая проблема: компилятору не хватает пути к заголовкам PostgreSQL (YugabyteDB) при проверке GDAL.

### 🛠️ Исправление: Добавьте путь к заголовкам YugabyteDB в CPPFLAGS

Нужно добавить этот путь в переменную `CPPFLAGS`. Запустите `configure` снова, но с исправленным параметром:

```bash
cd /tmp/postgis-3.3.8
make clean

./configure \
  --without-pgconfig \
  --with-geosconfig=/bin/geos-config \
  --with-projdir=/usr \
  --with-gdalconfig=/bin/gdal-config \
  --with-xml2config=/bin/xml2-config \
  LDFLAGS="-L/_a/yugabyte-2025.2.0.1/postgres/lib -Wl,-rpath,/_a/yugabyte-2025.2.0.1/postgres/lib" \
  CPPFLAGS="-I/_a/yugabyte-2025.2.0.1/postgres/include -I/usr/include"
```

**Что изменилось:**
1.  Убраны нераспознанные опции `--with-pgincdir` и `--with-pglibdir`.
2.  В `CPPFLAGS` добавлен `-I/usr/include` (стандартный путь для системных заголовков, где, вероятно, лежат заголовки GDAL). Теперь компилятор будет искать заголовки и в каталоге YugabyteDB, и в системных путях.
3.  Параметры для GEOS, PROJ, GDAL и libxml2 остаются, так как они уже настроены.

### 🔍 Проверка установки GDAL

Если после этого ошибка с заголовками GDAL останется, проверьте, действительно ли установлен пакет `gdal-devel` и где лежат его заголовки:

```bash
# Проверьте наличие заголовочного файла gdal.h
find /usr -name "gdal.h" 2>/dev/null

# Если файл найден (например, /usr/include/gdal/gdal.h),
# добавьте этот конкретный путь в CPPFLAGS:
CPPFLAGS="-I/_a/yugabyte-2025.2.0.1/postgres/include -I/usr/include -I/usr/include/gdal"
```

### ⚠️ Что делать, если `configure` всё равно завершится с "PostgreSQL support disabled"
Это ожидаемо, так как мы отключили `pg_config`. Главное, чтобы `configure` прошёл все проверки зависимостей и завершился с выводом **сводной таблицы** (как в прошлый успешный раз), где будут перечислены найденные версии GEOS, PROJ, GDAL и т.д. После этого можно запускать `make`.

**Попробуйте запустить исправленную команду `configure`.** Если она завершится успешно и покажет сводную таблицу — вы на финишной прямой. Если же будет новая ошибка (скорее всего, с какой-то другой библиотекой), сообщите её текст.
