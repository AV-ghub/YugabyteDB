```
configure: error: could not find libpq
```

Это известная проблема: `configure` пытается найти и линковаться с библиотекой `libpq`, которая является частью YugabyteDB, но из-за различий в путях или зависимостях это не удаётся.

### 🎯 Решение: Собрать PostGIS, обойдя проверку `libpq`

Вместо того чтобы пытаться заставить `configure` найти `libpq`, мы просто **обманем проверку** и явно укажем все пути. Мы уже делали нечто подобное для CentOS 7, но теперь все зависимости (`geos-config`, `gdal-config` и т.д.) уже установлены в системе.

Выполните эту команду в директории `/tmp/postgis-3.3.8`:

```bash
# 1. Очистите предыдущую конфигурацию
make clean

# 2. Запустите configure с полным набором параметров, обходящих проверку libpq
./configure \
  --without-pgconfig \
  --with-pgincdir=/_a/yugabyte-2025.2.0.1/postgres/include \
  --with-pglibdir=/_a/yugabyte-2025.2.0.1/postgres/lib \
  --with-geosconfig=/bin/geos-config \
  --with-projdir=/usr \
  --with-gdalconfig=/bin/gdal-config \
  --with-xml2config=/bin/xml2-config \
  LDFLAGS="-L/_a/yugabyte-2025.2.0.1/postgres/lib -Wl,-rpath,/_a/yugabyte-2025.2.0.1/postgres/lib" \
  CPPFLAGS="-I/_a/yugabyte-2025.2.0.1/postgres/include"
```

**Что это делает:**
- `--without-pgconfig` отключает попытку запустить `pg_config`.
- `--with-pgincdir` и `--with-pglibdir` указывают компилятору, где искать заголовочные файлы и библиотеки YugabyteDB.
- Остальные параметры (`--with-geosconfig` и т.д.) указывают на уже установленные в системе инструменты.
- `LDFLAGS` и `CPPFLAGS` гарантируют, что компоновщик и препроцессор будут искать библиотеки и заголовки в каталогах YugabyteDB.

### ✅ После успешного `configure`
Если `configure` завершится успешно (вы увидите сводную таблицу с версиями), выполните:
```bash
make
```
После успешной сборки остановите кластер, скопируйте новую библиотеку `postgis-3.so` и запустите кластер заново.

### 🔄 Если не сработает
В редких случаях может потребоваться явно указать библиотеку `libpq` во флагах линковки. Тогда попробуйте добавить `-lpq` в `LDFLAGS`:
```bash
LDFLAGS="-L/_a/yugabyte-2025.2.0.1/postgres/lib -Wl,-rpath,/_a/yugabyte-2025.2.0.1/postgres/lib -lpq"
```

**Попробуйте выполнить длинную команду `configure`. Сообщите, пройдёт ли она до конца.**

```
./configure \
  --without-pgconfig \
  --with-pgincdir=/_a/yugabyte-2025.2.0.1/postgres/include \
  --with-pglibdir=/_a/yugabyte-2025.2.0.1/postgres/lib \
  --with-geosconfig=/bin/geos-config \
  --with-projdir=/usr \
  --with-gdalconfig=/bin/gdal-config \
  --with-xml2config=/bin/xml2-config \
  LDFLAGS="-L/_a/yugabyte-2025.2.0.1/postgres/lib -Wl,-rpath,/_a/yugabyte-2025.2.0.1/postgres/lib" \
  CPPFLAGS="-I/_a/yugabyte-2025.2.0.1/postgres/include"
configure: WARNING: unrecognized options: --with-pgincdir, --with-pglibdir

configure: WARNING: xml2pot is not installed so translations cannot be updated
checking for msgmerge... /bin/msgmerge
checking for msginit... /bin/msginit
checking for convert... no
configure: WARNING: ImageMagick does not seem to be installed. Documentation cannot be built
checking for dblatex... no
configure: WARNING: dblatex is not installed so PDF documentation cannot be built
checking for xmllint... /bin/xmllint
configure: WARNING: could not locate Docbook stylesheets required to build the documentation
checking for pkg-config... /bin/pkg-config
checking pkg-config is at least version 0.9.0... yes
checking for CUNIT... no
configure: WARNING: could not locate CUnit required for unit tests

checking for libiconvctl... no
PostgreSQL support disabled
Using user-specified xml2-config file: /bin/xml2-config

configure: WARNING: "Could not find json-c"

ADDRESS_STANDARDIZER support: disabled
TOPOLOGY: Topology support requested
RASTER: Raster support requested
checking GDAL version... 3.10.3
checking for OGR enabled... yes
./configure: line 20465: no: command not found
./configure: line 20467: no: command not found

configure: WARNING: gdal.h: present but cannot be compiled
configure: WARNING: gdal.h:     check for missing prerequisite headers?
configure: WARNING: gdal.h: see the Autoconf documentation
configure: WARNING: gdal.h:     section "Present But Cannot Be Compiled"
configure: WARNING: gdal.h: proceeding with the compiler's result
checking for gdal.h... no
configure: error: could not find GDAL headers
```

Отлично, мы близки! Главное — **PostgreSQL support disabled** в логе означает, что `configure` принял наши флаги, но не понял нестандартные опции `--with-pgincdir` и `--with-pglibdir`. Это нормально. Ключевая ошибка теперь связана с **GDAL**: система нашла `gdal-config`, но не может скомпилировать проверочный код из-за отсутствия заголовков PostgreSQL.

Это классическая проблема: компилятору не хватает пути к заголовкам PostgreSQL (YugabyteDB) при проверке GDAL.

### 🛠️ Исправление: Добавьте путь к заголовкам YugabyteDB в CPPFLAGS

Нужно добавить этот путь в переменную `CPPFLAGS`. Запустите `configure` снова, но с исправленным параметром:

```bash
cd /tmp/postgis-3.3.8
make clean

./configure \
  --without-pgconfig \
  --with-geosconfig=/bin/geos-config \
  --with-projdir=/usr \
  --with-gdalconfig=/bin/gdal-config \
  --with-xml2config=/bin/xml2-config \
  LDFLAGS="-L/_a/yugabyte-2025.2.0.1/postgres/lib -Wl,-rpath,/_a/yugabyte-2025.2.0.1/postgres/lib" \
  CPPFLAGS="-I/_a/yugabyte-2025.2.0.1/postgres/include -I/usr/include"
```

**Что изменилось:**
1.  Убраны нераспознанные опции `--with-pgincdir` и `--with-pglibdir`.
2.  В `CPPFLAGS` добавлен `-I/usr/include` (стандартный путь для системных заголовков, где, вероятно, лежат заголовки GDAL). Теперь компилятор будет искать заголовки и в каталоге YugabyteDB, и в системных путях.
3.  Параметры для GEOS, PROJ, GDAL и libxml2 остаются, так как они уже настроены.

### 🔍 Проверка установки GDAL

Если после этого ошибка с заголовками GDAL останется, проверьте, действительно ли установлен пакет `gdal-devel` и где лежат его заголовки:

```bash
# Проверьте наличие заголовочного файла gdal.h
find /usr -name "gdal.h" 2>/dev/null

# Если файл найден (например, /usr/include/gdal/gdal.h),
# добавьте этот конкретный путь в CPPFLAGS:
CPPFLAGS="-I/_a/yugabyte-2025.2.0.1/postgres/include -I/usr/include -I/usr/include/gdal"
```

### ⚠️ Что делать, если `configure` всё равно завершится с "PostgreSQL support disabled"
Это ожидаемо, так как мы отключили `pg_config`. Главное, чтобы `configure` прошёл все проверки зависимостей и завершился с выводом **сводной таблицы** (как в прошлый успешный раз), где будут перечислены найденные версии GEOS, PROJ, GDAL и т.д. После этого можно запускать `make`.

**Попробуйте запустить исправленную команду `configure`.** Если она завершится успешно и покажет сводную таблицу — вы на финишной прямой. Если же будет новая ошибка (скорее всего, с какой-то другой библиотекой), сообщите её текст.

```
нет, все то же самое
find /usr -name "gdal.h" 2>/dev/null
/usr/include/gdal/gdal.h
```

Проблема ясна: заголовочный файл `gdal.h` находится в подкаталоге `/usr/include/gdal/`, и компилятор не может его найти по стандартному пути. Нужно явно добавить этот подкаталог в `CPPFLAGS`.

### ✅ Исправленная команда configure

Выполните следующую команду, обращая внимание на последний параметр `CPPFLAGS`:

```bash
cd /tmp/postgis-3.3.8
make clean

./configure \
  --without-pgconfig \
  --with-geosconfig=/bin/geos-config \
  --with-projdir=/usr \
  --with-gdalconfig=/bin/gdal-config \
  --with-xml2config=/bin/xml2-config \
  LDFLAGS="-L/_a/yugabyte-2025.2.0.1/postgres/lib -Wl,-rpath,/_a/yugabyte-2025.2.0.1/postgres/lib" \
  CPPFLAGS="-I/_a/yugabyte-2025.2.0.1/postgres/include -I/usr/include/gdal -I/usr/include"
```

**Ключевое изменение:** в `CPPFLAGS` добавлен путь `-I/usr/include/gdal`. Это нужно для проверки заголовков GDAL. Флаг `-I/usr/include` оставлен для поиска других системных заголовков.

### 💡 Если это не сработает

В редких случаях может потребоваться также проверить версию и целостность установки `gdal-devel`. Убедитесь, что пакет установлен полностью:

```bash
# Проверьте установленные файлы пакета gdal-devel
rpm -ql gdal-devel | grep -E "(include|gdal\.h)"
```

### 🎯 Что дальше

Если `configure` завершится успешно (вы увидите сводную таблицу с версиями библиотек), запустите `make`. После успешной сборки:

1.  **Остановите кластер YugabyteDB.**
2.  **Скопируйте новую библиотеку:**
    ```bash
    sudo cp ./postgis-3.so /_a/yugabyte-2025.2.0.1/postgres/lib/
    ```
3.  **Запустите кластер.**
4.  **Проверьте расширение:**
    ```sql
    CREATE EXTENSION postgis;
    SELECT postgis_version();
    ```

На этот раз библиотека собрана с использованием путей и заголовков именно от YugabyteDB, что должно гарантировать бинарную совместимость.

**Попробуйте исправленную команду и сообщите результат. Если `configure` пройдёт, вы практически у цели.**

```
то же
checking for gdal.h... no
configure: error: could not find GDAL headers
```

Проблема с GDAL сохраняется, несмотря на добавление путей. Давайте подойдем к этому системно и проверим все возможные причины.

### 🔍 Глубокий анализ и решение
1.  **Проверьте, что `gdal-devel` действительно установлен правильно**:
    ```bash
    # Переустановите пакет для уверенности
    sudo dnf reinstall -y gdal-devel

    # Убедитесь, что заголовочные файлы на месте
    ls -la /usr/include/gdal/gdal.h
    ```

2.  **Попробуйте передать путь к заголовкам GDAL через отдельную переменную `GDAL_CFLAGS`**. Иногда `configure` требует именно такой формат:
    ```bash
    GDAL_CFLAGS="-I/usr/include/gdal" ./configure \
      --without-pgconfig \
      --with-geosconfig=/bin/geos-config \
      --with-projdir=/usr \
      --with-gdalconfig=/bin/gdal-config \
      --with-xml2config=/bin/xml2-config \
      LDFLAGS="-L/_a/yugabyte-2025.2.0.1/postgres/lib -Wl,-rpath,/_a/yugabyte-2025.2.0.1/postgres/lib" \
      CPPFLAGS="-I/_a/yugabyte-2025.2.0.1/postgres/include -I/usr/include/gdal -I/usr/include"
    ```

3.  **Создайте символическую ссылку на `gdal.h` в `/usr/include`**. Это самый прямой, хоть и немного "грубый", способ заставить компилятор найти файл:
    ```bash
    sudo ln -sf /usr/include/gdal/gdal.h /usr/include/gdal.h
    ```
    После этого снова запустите `configure` (без изменения `CPPFLAGS`, так как `/usr/include` уже указан).

4.  **Проверьте наличие других критически важных заголовочных файлов GDAL** (помимо `gdal.h`):
    ```bash
    # Например, gdal_version.h
    find /usr/include/gdal -name "*.h" | head -10
    ```

### 📊 Альтернативный подход: отключение растровой поддержки
Если проблема не решается, и растровая поддержка PostGIS вам **не критична**, вы можете **полностью отключить** её, что избавит от необходимости искать GDAL:
```bash
./configure \
  --without-pgconfig \
  --without-raster \
  --with-geosconfig=/bin/geos-config \
  --with-projdir=/usr \
  --with-xml2config=/bin/xml2-config \
  LDFLAGS="-L/_a/yugabyte-2025.2.0.1/postgres/lib -Wl,-rpath,/_a/yugabyte-2025.2.0.1/postgres/lib" \
  CPPFLAGS="-I/_a/yugabyte-2025.2.0.1/postgres/include -I/usr/include"
```
**Обратите внимание:** `--without-raster` убирает зависимость от GDAL, `--with-gdalconfig` также убран.

### 🚀 Рекомендация
Сначала попробуйте **переустановить `gdal-devel` и создать символическую ссылку**. Это часто решает проблему в застрявших системах. Если не сработает — оцените, нужна ли вам растровая функциональность. Если нет, вариант с `--without-raster` гарантированно продвинет сборку дальше.

**Попробуйте шаги 1 и 3 из глубокого анализа. Сообщите, что получится.**

```
sudo dnf reinstall -y gdal-devel
Repository epel is listed more than once in the configuration
modern-erlang-el8                                                                                                                                                           1.9 kB/s | 659  B     00:00
modern-erlang-el8-noarch                                                                                                                                                    2.0 kB/s | 659  B     00:00
modern-erlang-el8-source                                                                                                                                                    2.1 kB/s | 659  B     00:00
rabbitmq-el8                                                                                                                                                                928  B/s | 659  B     00:00
rabbitmq-el8-noarch                                                                                                                                                         975  B/s | 659  B     00:00
rabbitmq-el8-source                                                                                                                                                         1.3 kB/s | 659  B     00:00
Dependencies resolved.
============================================================================================================================================================================================================
 Package                                            Architecture                                   Version                                                Repository                                   Size
============================================================================================================================================================================================================
Reinstalling:
 gdal-devel                                         x86_64                                         3.10.3-3.el9                                           crb                                         219 k

Transaction Summary
============================================================================================================================================================================================================

Total download size: 219 k
Installed size: 1.3 M
Downloading Packages:
[MIRROR] gdal-devel-3.10.3-3.el9.x86_64.rpm: Curl error (35): SSL connect error for https://us.mirrors.cicku.me/rocky/9.7/CRB/x86_64/os/Packages/g/gdal-devel-3.10.3-3.el9.x86_64.rpm [OpenSSL SSL_connect: SSL_ERROR_ZERO_RETURN in connection to us.mirrors.cicku.me:443 ]
gdal-devel-3.10.3-3.el9.x86_64.rpm                                                                                                                                           13 kB/s | 219 kB     00:16
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Total                                                                                                                                                                        13 kB/s | 219 kB     00:17
Running transaction check
Transaction check succeeded.
Running transaction test
Transaction test succeeded.
Running transaction
  Preparing        :                                                                                                                                                                                    1/1
  Reinstalling     : gdal-devel-3.10.3-3.el9.x86_64                                                                                                                                                     1/2
  Cleanup          : gdal-devel-3.10.3-3.el9.x86_64                                                                                                                                                     2/2
  Running scriptlet: gdal-devel-3.10.3-3.el9.x86_64                                                                                                                                                     2/2
  Verifying        : gdal-devel-3.10.3-3.el9.x86_64                                                                                                                                                     1/2
  Verifying        : gdal-devel-3.10.3-3.el9.x86_64                                                                                                                                                     2/2

Reinstalled:
  gdal-devel-3.10.3-3.el9.x86_64

Complete!


ls -la /usr/include/gdal/gdal.h
-rw-r--r-- 1 root root 124635 Apr  1  2025 /usr/include/gdal/gdal.h

GDAL_CFLAGS="-I/usr/include/gdal" ./configure \
  --without-pgconfig \
  --with-geosconfig=/bin/geos-config \
  --with-projdir=/usr \
  --with-gdalconfig=/bin/gdal-config \
  --with-xml2config=/bin/xml2-config \
  LDFLAGS="-L/_a/yugabyte-2025.2.0.1/postgres/lib -Wl,-rpath,/_a/yugabyte-2025.2.0.1/postgres/lib" \
  CPPFLAGS="-I/_a/yugabyte-2025.2.0.1/postgres/include -I/usr/include/gdal -I/usr/include"

configure: error: could not find GDAL headers

sudo ln -sf /usr/include/gdal/gdal.h /usr/include/gdal.h

GDAL_CFLAGS="-I/usr/include/gdal" ./configure \ ...
configure: error: could not find GDAL headers

find /usr/include/gdal -name "*.h" | head -10
/usr/include/gdal/cpl_minixml.h
/usr/include/gdal/cpl_multiproc.h
/usr/include/gdal/cpl_odbc.h
/usr/include/gdal/cpl_port.h
/usr/include/gdal/cpl_progress.h
/usr/include/gdal/cpl_quad_tree.h
/usr/include/gdal/cpl_spawn.h
/usr/include/gdal/cpl_string.h
/usr/include/gdal/cpl_time.h
/usr/include/gdal/cpl_virtualmem.h

так
./configure \
  --without-pgconfig \
  --without-raster \
  --with-geosconfig=/bin/geos-config \
  --with-projdir=/usr \
  --with-xml2config=/bin/xml2-config \
  LDFLAGS="-L/_a/yugabyte-2025.2.0.1/postgres/lib -Wl,-rpath,/_a/yugabyte-2025.2.0.1/postgres/lib" \
  CPPFLAGS="-I/_a/yugabyte-2025.2.0.1/postgres/include -I/usr/include"

в итоге собралось
```

**Отлично! 🎉**

Сборка конфигурации завершилась успешно! Это значит, что **все необходимые библиотеки найдены и готовы к компиляции**.

Вы пошли по пути `--without-raster`, то есть **отключили растровую поддержку PostGIS**. Это абсолютно валидное решение, если вам не нужны функции работы с растровыми данными (`ST_FromGDALRaster`, `ST_AsGDALRaster` и т.д.). Для большинства операций с векторными данными (геометрии, топология) это не критично.

### 🚀 **Следующие шаги**

Теперь вам нужно выполнить стандартную последовательность действий. Не забудьте про необходимость остановки кластера перед копированием библиотеки.

1.  **Скомпилируйте PostGIS:**
    ```bash
    cd /tmp/postgis-3.3.8
    make
    ```

2.  **Остановите кластер YugabyteDB.**

3.  **Скопируйте новую, совместимую библиотеку** `postgis-3.so`, которая появится в текущей директории после успешного `make`:
    ```bash
    sudo cp postgis-3.so /_a/yugabyte-2025.2.0.1/postgres/lib/
    ```

4.  **Запустите кластер YugabyteDB.**

5.  **Проверьте установку расширения,** подключившись через `ysqlsh`:
    ```sql
    CREATE EXTENSION postgis;
    SELECT postgis_version();
    ```

### 📝 **Важные моменты**

*   Эта библиотека собрана **специально для вашей среды YugabyteDB** (с использованием его путей в `CPPFLAGS`/`LDFLAGS`), поэтому шансы на совместимость максимальны.
*   Поскольку вы использовали `--without-raster`, в сводной таблице после `make install` вы увидите `PostGIS Raster: disabled`. Это ожидаемо.
*   Для большинства геопространственных задач (работа с точками, линиями, полигонами, топологией) отключение растров не является проблемой.

**Приступайте к команде `make`. Если она завершится успешно, вы практически у цели.** После этого выполните копирование библиотеки, перезапуск кластера и финальную проверку.

```
make
PostGIS was built successfully. Ready to install.

sudo cp postgis-3.so /_a/yugabyte-2025.2.0.1/postgres/lib/
cp: cannot stat 'postgis-3.so': No such file or directory
```

Библиотека `postgis-3.so` не находится в текущей директории. После успешного `make` она должна находиться в **подкаталоге сборки**, обычно в `./lib/` или `./src/`.

### 🔍 Найдите правильный путь к библиотеке

Выполните команду поиска из корневой директории PostGIS:

```bash
find /tmp/postgis-3.3.8 -name "postgis-3.so" -type f
```

Скорее всего, она найдётся по одному из этих путей:
- `/tmp/postgis-3.3.8/src/postgis-3.so`
- `/tmp/postgis-3.3.8/lib/postgis-3.so`
- `/tmp/postgis-3.3.8/.libs/postgis-3.so`

### ✅ Правильная команда копирования

После того как вы найдёте точный путь, скопируйте библиотеку:

```bash
# Например, если библиотека в src/
sudo cp /tmp/postgis-3.3.8/src/postgis-3.so /_a/yugabyte-2025.2.0.1/postgres/lib/

# Или если в .libs/
sudo cp /tmp/postgis-3.3.8/.libs/postgis-3.so /_a/yugabyte-2025.2.0.1/postgres/lib/
```

### 🚀 Далее: проверка работы

После копирования убедитесь, что файл на месте, остановите кластер YugabyteDB, запустите его снова и выполните финальную проверку:

```sql
CREATE EXTENSION postgis;
SELECT postgis_version();
```

**Найдите библиотеку командой `find` и скопируйте её. Сообщите результат.**

```
нет
find /tmp/postgis-3.3.8 -name "postgis-3.so" -type f
пусто

и путей таких
/tmp/postgis-3.3.8/src/postgis-3.so
/tmp/postgis-3.3.8/lib/postgis-3.so
/tmp/postgis-3.3.8/.libs/postgis-3.so
нет
```
`make` сообщил об успехе, но не создал нужный файл `postgis-3.so`. Похоже, процесс сборки остановился после создания инструментов командной строки (например, `shp2pgsql`) и не дошёл до компиляции самого основного расширения для PostgreSQL.

Основная причина, вероятно, в использовании ключевых флагов `--without-pgconfig` и `--without-raster`. Они сообщили системе сборки PostGIS, что поддержка интеграции с PostgreSQL **отключена**, поэтому она не стала компилировать главный модуль расширения.

### 🔍 Проверка и поиск файлов
Давайте посмотрим, какие файлы всё же были созданы.

```bash
cd /tmp/postgis-3.3.8
find . -name "*.so" -type f
```
Если вы увидите файлы вроде `liblwgeom.a` или `libpgcommon.a` — это библиотеки для внутреннего использования, а не финальное расширение `postgis-3.so`.

### ✅ Как исправить и создать `postgis-3.so`
Библиотека `postgis-3.so` создаётся, когда `make` компилирует часть `postgis/`. Для этого **необходимо** правильно указать PostGIS путь к PostgreSQL. Поскольку попытка использования `pg_config` из YugabyteDB вызывала проблемы, лучший путь — вернуться к стандартной сборке для **системного PostgreSQL 15** и использовать `--without-raster` только для отключения GDAL.

Выполните следующую последовательность команд:

```bash
cd /tmp/postgis-3.3.8

# 1. Очистите результаты всех предыдущих попыток сборки
make distclean

# 2. Запустите configure с указанием на рабочий pg_config,
#    но без поддержки растров (для обхода проблемы с GDAL)
./configure --with-pgconfig=/usr/pgsql-15/bin/pg_config --without-raster

# 3. Запустите сборку
make
```
Эта команда `configure` должна пройти все проверки (GEOS, PROJ), а `make` создаст библиотеку `postgis-3.so`, расположение которой можно найти через `find . -name "postgis-3.so" -type f`. Наиболее вероятный путь — `./src/postgis-3.so`.

### 💡 Почему это должно сработать
*   Вы используете `pg_config` от стандартного PostgreSQL 15, который корректно указывает на все необходимые для сборки файлы.
*   Флаг `--without-raster` отключает только проблемную зависимость от GDAL, не затрагивая основную сборку векторного расширения PostGIS, которую вам и нужно получить.

После успешного `make` найдите файл `postgis-3.so`, скопируйте его в YugabyteDB и проверьте работу расширения.

Попробуйте этот подход — он позволит получить работоспособную библиотеку. Сообщите о результате.
