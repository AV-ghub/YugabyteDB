```
docker build -t yugabyte-with-postgis-test:latest .
Sending build context to Docker daemon  22.53kB
Step 1/8 : FROM rockylinux:8 as postgis-builder
8: Pulling from library/rockylinux
9088cdb84e39: Retrying in 1 second
error pulling image configuration: download failed after attempts=6: x509: certificate signed by unknown authority

```

Сетевые проблемы при попытке скачать базовый образ. Ошибка указывает на **проблему с SSL-сертификатами на вашем хост-машине**, где запущен Docker Daemon. Вероятнее всего, это связано с корпоративным прокси или внутренним реестром образов.

Попробуйте следующие решения в порядке возрастания сложности:

### ✅ Способ 1: Использование публичного реестра без HTTPS

Используйте образ с Docker Hub через HTTP (если политика безопасности позволяет). Для этого **настройте Docker Daemon** на вашей хост-машине:

1.  **Отредактируйте или создайте файл конфигурации Docker Daemon** `/etc/docker/daemon.json`:
    ```json
    {
        "insecure-registries": ["docker.io"]
    }
    ```
2.  **Перезапустите Docker**:
    ```bash
    sudo systemctl restart docker
    ```
3.  **Повторите сборку**.

### 🔧 Способ 2: Ручная загрузка и импорт образа

Если настройка Daemon невозможна, скачайте образ вручную на машину с рабочим интернетом и перенесите его.

```bash
# На машине с доступом в интернет (например, вашем ноутбуке):
docker pull rockylinux:8
docker save rockylinux:8 -o rockylinux_8.tar

# Перенесите файл rockylinux_8.tar на хост-машину (через scp, usb-накопитель и т.д.)

# На вашей хост-машине с YugabyteDB:
docker load -i rockylinux_8.tar

# Убедитесь, что образ загружен:
docker images | grep rockylinux

# Теперь повторите сборку вашего Dockerfile
docker build -t yugabyte-with-postgis-test:latest .
```

### 💡 Способ 3: Обходной путь — использовать другой базовый образ

Попробуйте использовать другой популярный базовый образ для первого этапа, например `centos:7` (хотя он старый) или `ubuntu:20.04`, который может быть в вашем локальном кэше или реестре.

Замените **первую строку** вашего Dockerfile на одну из этих:
```dockerfile
FROM centos:7 as postgis-builder
# ИЛИ
FROM ubuntu:20.04 as postgis-builder
```

Если выберете `ubuntu:20.04`, вам нужно будет адаптировать команды установки (использовать `apt` вместо `yum/dnf`).

### 🚀 Краткий план действий

1.  **Попробуйте Способ 3 (самый быстрый)** — замените `rockylinux:8` на `centos:7` в Dockerfile и попробуйте собрать заново.
2.  **Если не сработает** — попробуйте Способ 2 (ручная загрузка образа).
3.  **Если у вас есть доступ к администрированию Docker** — используйте Способ 1.

