### 🚀 Стартуем с PostGIS (Задача №2)

Цель на ближайшие **3 дня**: Убедиться, что PostGIS работает в распределённом кластере YugabyteDB, и выполнить базовые операции.

**Практическое задание №1: Установка и проверка**

```bash
# 1. Подключитесь к кластеру
psql -h localhost -p 5433 -U yugabyte

-- 2. Создайте БД для геоданных (опционально, но рекомендуется)
CREATE DATABASE geo_test;
\c geo_test

-- 3. Создайте расширение PostGIS (ключевой шаг!)
CREATE EXTENSION postgis;

-- 4. Проверьте установку и версию
SELECT postgis_version();
SELECT PostGIS_Full_Version();
```

**Практическое задание №2: Базовые гео-операции**

```sql
-- 1. Создайте таблицу с гео-объектами
CREATE TABLE geographical_objects (
    id SERIAL PRIMARY KEY,
    name TEXT,
    -- Типы данных PostGIS:
    location GEOMETRY(Point, 4326),        -- Точка (долгота/широта)
    area GEOMETRY(Polygon, 4326),          -- Полигон
    description TEXT
);

-- 2. Вставьте тестовые данные
INSERT INTO geographical_objects (name, location, area) VALUES
    ('Центр города',
     ST_SetSRID(ST_MakePoint(37.6178, 55.7558), 4326), -- Москва
     ST_MakePolygon(ST_GeomFromText('LINESTRING(37.6 55.7, 37.7 55.7, 37.7 55.8, 37.6 55.8, 37.6 55.7)', 4326))),
    ('Аэропорт',
     ST_SetSRID(ST_MakePoint(37.4145, 55.9728), 4326), -- Шереметьево
     NULL);

-- 3. Выполните пространственный запрос
-- Найти объекты в радиусе 50 км от точки
SELECT name,
       ST_AsText(location) as coordinates,
       ST_Distance(
           location::geography,
           ST_SetSRID(ST_MakePoint(37.6178, 55.7558), 4326)::geography
       ) / 1000 as distance_km
FROM geographical_objects
WHERE ST_DWithin(
    location::geography,
    ST_SetSRID(ST_MakePoint(37.6178, 55.7558), 4326)::geography,
    50000 -- 50 км в метрах
);
```

### 📊 Что важно зафиксировать в первые дни (для будущего отчёта):
1.  **Установка**: Прошла ли установка расширения без ошибок?
2.  **Поддерживаемые типы данных**: Работают ли `Point`, `Polygon`, `LineString`?
3.  **Базовые функции**: Выполняются ли `ST_Distance`, `ST_Within`, `ST_Intersects`?
4.  **Особенность YugabyteDB**: Как повела себя таблица? Можно посмотреть в веб-интерфейсе (`http://localhost:7000`), на сколько таблетов она разбилась?
