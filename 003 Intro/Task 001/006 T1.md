Отличный прогресс! Пакет найден, но система столкнулась с **конфликтом модулей DNF/YUM**. Это типичная ситуация, когда в системе одновременно доступны стандартный модуль `postgresql` от дистрибутива и пакеты из внешнего репозитория PostgreSQL (PGDG). Чтобы это исправить, нужно полностью **отключить модуль** PostgreSQL от AlmaLinux. Из лога видно, что команда `yum module disable -y postgresql` уже была выполнена ранее, но в случае с DNF может потребоваться немного другой подход.

### 🛠️ Решение конфликта модулей

Войдите в контейнер и выполните следующие команды:

```bash
# 1. Полностью сбросить состояние модуля PostgreSQL
dnf module reset -y postgresql

# 2. Убедиться, что модуль отключен (должна быть строка "[d]")
dnf module list postgresql

# 3. Установить PostGIS с флагами, игнорирующими конфликты модулей
dnf install -y postgis33_15 --allowerasing --skip-broken
```

Если `dnf module reset` не сработает, можно использовать более агрессивный подход:

```bash
# Удалить файлы конфигурации модуля
rm -f /etc/dnf/modules.d/postgresql.module
# Перестроить кэш
dnf clean all && dnf makecache
# Повторить установку
dnf install -y postgis33_15
```

### 📊 Альтернативные варианты решения

Если проблема с модулями не решится, есть другие пути:

| Подход | Команды | Когда использовать |
|--------|---------|-------------------|
| **Установка через модуль** | `dnf module install -y postgresql:15/postgis` | Если в модуле есть PostGIS |
| **Минимальная установка** | `dnf install -y postgis33_15 --exclude=postgresql15-contrib*` | Для тестов, без `contrib` |
| **Поиск другого пакета** | `dnf search postgis | grep 15` | Поиск альтернативных вариантов |

### 🔍 Проверьте наличие PostGIS в модуле

Сначала проверьте, есть ли PostGIS в стандартном модуле:

```bash
dnf module info postgresql:15
```

Если в выводе есть упоминание `postgis`, можно установить через модуль:

```bash
dnf module install -y postgresql:15/postgis
```

### ⚡ Быстрое решение для тестов

Если нужно просто протестировать работу PostGIS в YugabyteDB, можно попробовать установить без `contrib`:

```bash
dnf install -y postgis33_15 --exclude=postgresql15-contrib*
```

После установки продолжите с копированием файлов:

```bash
cp -r /usr/pgsql-15/share/extension/*postgis* /home/yugabyte/share/postgresql/extension/
cp -r /usr/pgsql-15/share/contrib/postgis-3.3 /home/yugabyte/share/postgresql/contrib/
cp /usr/pgsql-15/lib/postgis-3.3.so /home/yugabyte/lib/postgresql/
```

### 📝 Что показывает ошибка

Ключевая фраза в ошибке: **`is filtered out by modular filtering`**. Это означает, что:
1. В системе есть модуль `postgresql` от AlmaLinux
2. Этот модуль блокирует установку пакетов из репозитория PGDG
3. Нужно полностью отключить или сбросить модуль

### ✅ Дальнейшие шаги

1. Попробуйте **`dnf module reset -y postgresql`** 
2. Если не сработает — **удалите файл модуля** и перестройте кэш
3. В крайнем случае — **установите без contrib**

**После успешной установки** не забудьте:
1. Скопировать файлы PostGIS в директории YugabyteDB
2. Вернуть настройку `sslverify=1` в `/etc/yum.conf`
3. Проверить создание расширения: `ysqlsh -U yugabyte -d geo_test -c "CREATE EXTENSION postgis;"`

**Попробуйте сначала сброс модуля через `dnf module reset` и покажите результат.**
