**Это не просто возможно — это самый прямой и надёжный способ** для вашей задачи, особенно учитывая сроки! Процесс сборки своего образа решает все проблемы с зависимостями и совместимостью.

Давайте создадим **Dockerfile для вашей точной версии YugabyteDB**.

### 🐳 Создание Dockerfile

Создайте в вашей рабочей директории файл `Dockerfile.yugabyte-postgis`:

```dockerfile
# Используем ОФИЦИАЛЬНЫЙ образ YugabyteDB как основу
FROM yugabytedb/yugabyte:latest

# Устанавливаем PostGIS (ваш контейнер на базе AlmaLinux 8)
USER root

# 1. Устанавливаем репозиторий PostgreSQL для PostGIS
RUN dnf install -y https://download.postgresql.org/pub/repos/yum/reporpms/EL-8-x86_64/pgdg-redhat-repo-latest.noarch.rpm

# 2. Отключаем стандартный модуль PostgreSQL (важно для AlmaLinux 8)
RUN dnf -qy module disable postgresql

# 3. Устанавливаем PostGIS для PostgreSQL 15 (совместим с YugabyteDB)
RUN dnf install -y postgis31_15

# 4. Копируем файлы PostGIS в директории YugabyteDB
RUN cp -r /usr/pgsql-15/share/extension/*postgis* /home/yugabyte/share/postgresql/extension/ && \
    cp -r /usr/pgsql-15/share/contrib/postgis-3.1 /home/yugabyte/share/postgresql/contrib/ && \
    cp /usr/pgsql-15/lib/postgis-3.1.so /home/yugabyte/lib/postgresql/

# Возвращаемся к пользователю yugabyte для безопасности
USER yugabyte

# Проверяем, что файлы на месте
RUN ls -la /home/yugabyte/share/postgresql/extension/*postgis* && \
    ls -la /home/yugabyte/share/postgresql/contrib/postgis-3.1/
```

### 🏗️ Сборка образа

```bash
# Соберите образ (это займет 5-10 минут)
docker build -f Dockerfile.yugabyte-postgis -t yugabyte-postgis:local .

# Проверьте, что образ создан
docker images | grep yugabyte-postgis
```

### 🔄 Обновление docker-compose.yml

Замените образ во ВСЕХ сервисах (мастера и tserver'ы):

```yaml
# Было:
# image: yugabytedb/yugabyte:latest

# Стало:
image: yugabyte-postgis:local
```

### 🚀 Запуск и проверка

```bash
# 1. Полностью остановите старый кластер (с сохранением томов!)
docker-compose down

# 2. Запустите с новым образом
docker-compose up -d

# 3. Подождите 3 минуты для инициализации
sleep 180

# 4. Проверьте установку PostGIS
psql -h localhost -p 5433 -U yugabyte -d geo_test << 'EOF'
CREATE EXTENSION postgis;
SELECT postgis_version();
SELECT ST_MakePoint(37.6, 55.7) as point;
EOF
```

### 📊 Почему этот подход лучше других:

| Подход | Плюсы | Минусы | Рекомендация |
|--------|-------|--------|--------------|
| **Сборка своего образа** | Полный контроль, совместимость, повторяемость | 10-15 минут на сборку | **Лучший выбор для production-тестов** |
| Сторонний образ с GitHub | Не нужно собирать | Риск устаревших версий, неясная поддержка | Только если нет времени на сборку |
| Установка в работающий контейнер | Быстро | Хрупко, проблемы с зависимостями, невоспроизводимо | Только для быстрых экспериментов |

### 🛠️ Если возникнут проблемы при сборке:

1. **Ошибка с репозиторием PostgreSQL**:
   ```dockerfile
   # Альтернатива: установка из стандартных репозиториев
   RUN dnf install -y postgis
   ```

2. **Проверка содержимого контейнера**:
   ```bash
   # Запустите временный контейнер для отладки
   docker run -it --rm yugabyte-postgis:local bash
   # Внутри проверьте:
   ls -la /home/yugabyte/share/postgresql/extension/ | grep -i postgis
   ```

3. **Версия PostGIS** - если нужна другая версия:
   ```dockerfile
   # Для PostGIS 3.3
   RUN dnf install -y postgis33_15
   ```

### 🎯 Ключевые преимущества вашего подхода:

1. **Контроль версий** - вы точно знаете, какая версия YugabyteDB и PostGIS используется
2. **Воспроизводимость** - Dockerfile можно сохранить в git
3. **Совместимость** - все библиотеки точно подходят для вашего образа
4. **Безопасность** - не используете сторонние образы неизвестного происхождения

**В вашем контейнере на базе AlmaLinux 8 нужно использовать `yum`**, а не `dnf`. `dnf` — это более новая версия менеджера пакетов в RHEL/AlmaLinux, но `yum` остаётся доступным и полностью рабочим.

Вот исправленный **Dockerfile**, который будет работать в вашем контейнере:

```dockerfile
FROM yugabytedb/yugabyte:latest

USER root

# 1. Устанавливаем репозиторий PostgreSQL для PostGIS через yum
RUN yum install -y https://download.postgresql.org/pub/repos/yum/reporpms/EL-8-x86_64/pgdg-redhat-repo-latest.noarch.rpm

# 2. Отключаем стандартный модуль PostgreSQL (важно для EL8)
RUN yum module disable -y postgresql

# 3. Устанавливаем PostGIS для PostgreSQL 15 через yum
RUN yum install -y postgis31_15

# 4. Копируем файлы PostGIS в директории YugabyteDB
RUN cp -r /usr/pgsql-15/share/extension/*postgis* /home/yugabyte/share/postgresql/extension/ && \
    cp -r /usr/pgsql-15/share/contrib/postgis-3.1 /home/yugabyte/share/postgresql/contrib/ && \
    cp /usr/pgsql-15/lib/postgis-3.1.so /home/yugabyte/lib/postgresql/

USER yugabyte
```

### 🔧 Важные уточнения по работе с `yum` в контейнере:

1. **Если будут проблемы с GPG-ключами**:
   ```dockerfile
   # Добавьте эту строку перед установкой
   RUN yum install -y --nogpgcheck https://download.postgresql.org/...
   ```

2. **Для ускорения установки** можно добавить флаги:
   ```dockerfile
   RUN yum install -y --setopt=skip_missing_names_on_install=False postgis31_15
   ```

3. **Проверка установленных пакетов**:
   ```bash
   # После сборки образа проверьте
   docker run --rm yugabyte-postgis:local yum list installed | grep -i postgis
   ```

### 📊 Сравнение `yum` vs `dnf` в вашем случае:

| Аспект | `yum` | `dnf` | Рекомендация |
|--------|-------|-------|--------------|
| **Доступность** | Есть в AlmaLinux 8 | Есть в AlmaLinux 8 | Используйте `yum` — вы уже с ним работали |
| **Совместимость** | Полная | Полная | Без разницы |
| **Производительность** | Немного медленнее | Быстрее | Для Docker-образа разница несущественна |
| **Знакомство** | Вы уже использовали | Новый синтаксис | `yum` проще для отладки |

### 🚀 Оптимизированный процесс сборки:

```bash
# 1. Создайте Dockerfile
cat > Dockerfile.yugabyte-postgis << 'EOF'
FROM yugabytedb/yugabyte:latest
USER root
RUN yum install -y https://download.postgresql.org/pub/repos/yum/reporpms/EL-8-x86_64/pgdg-redhat-repo-latest.noarch.rpm
RUN yum module disable -y postgresql
RUN yum install -y postgis31_15
RUN cp -r /usr/pgsql-15/share/extension/*postgis* /home/yugabyte/share/postgresql/extension/
RUN cp -r /usr/pgsql-15/share/contrib/postgis-3.1 /home/yugabyte/share/postgresql/contrib/
RUN cp /usr/pgsql-15/lib/postgis-3.1.so /home/yugabyte/lib/postgresql/
USER yugabyte
EOF

# 2. Соберите образ с кэшированием (быстрее при пересборках)
docker build -f Dockerfile.yugabyte-postgis -t yugabyte-postgis:local .

# 3. Быстрая проверка
docker run --rm yugabyte-postgis:local find /home/yugabyte -name "*postgis*" | head -10
```

### ⚠️ Возможные проблемы и решения:

1. **"Error: Failed to download metadata"** — проблема с SSL в контейнере:
   ```dockerfile
   # Решение: временно отключите SSL проверку
   RUN yum install -y --nogpgcheck ca-certificates
   ```

2. **Нет пакета `postgis31_15`** — попробуйте поискать доступные:
   ```bash
   # Запустите временный контейнер для поиска
   docker run --rm -it yugabytedb/yugabyte:latest yum search postgis
   ```

3. **Устаревшие метаданные** — обновите кэш:
   ```dockerfile
   RUN yum makecache
   ```

### ✅ Почему именно этот подход работает:

1. **Использует ту же ОС**, что и ваш текущий контейнер (AlmaLinux 8)
2. **Не требует изменений в `docker-compose.yml`** кроме имени образа
3. **Сохраняет все ваши настройки** (томы, порты, конфигурацию)
4. **Позволяет продолжить с того же места** — данные останутся в томах

**Начните сборку с исправленным Dockerfile.** Если будут ошибки — покажите полный вывод команды `docker build`. 

После успешной сборки вам останется только поменять `image:` в `docker-compose.yml` и запустить кластер заново.

**Отличная мысль! Вы абсолютно правы — это самый правильный и эффективный подход.**

Попробовать команды вручную в работающем T-контейнере — это **идеальная стратегия для отладки**. Вы сразу увидите все ошибки, найдёте правильные имена пакетов и поймёте логику копирования файлов.

### 🎯 Зачем это делать:

1. **Мгновенная обратная связь** — увидите ошибки сразу, а не после 10-минутной сборки
2. **Поиск реальных имён пакетов** — узнаете точное название PostGIS в репозиториях AlmaLinux 8
3. **Проверка путей** — убедитесь, куда именно нужно копировать файлы
4. **Тест работы PostGIS** — сразу проверите, создаётся ли расширение

### 🔧 Практический план отладки в контейнере:

```bash
# 1. Войдите в любой T-контейнер (например, yb-tserver-a)
docker exec -it yugabyte_yb-tserver-a_1 bash

# 2. Станьте root для установки пакетов
su - root
# или если не работает:
sudo bash
```

**Теперь выполняйте команды ПО ОЧЕРЕДИ и фиксируйте вывод:**

```bash
# 1. Обновление репозиториев
yum update -y

# 2. Установка репозитория PostgreSQL
yum install -y https://download.postgresql.org/pub/repos/yum/reporpms/EL-8-x86_64/pgdg-redhat-repo-latest.noarch.rpm

# 3. Поиск доступных пакетов PostGIS
yum search postgis
# ИЛИ более точный поиск:
yum list available | grep -i postgis

# 4. Установка PostGIS (используйте найденное имя пакета)
# Возможные варианты:
yum install -y postgis31_15      # Для PostgreSQL 15
yum install -y postgis33_15      # Если есть более новая версия
yum install -y postgis-client postgis-server  # Если нет номерных версий

# 5. После установки проверьте пути
find /usr -name "*postgis*" -type f 2>/dev/null | grep -E "(\.so|\.sql|\.control)$" | head -20

# 6. Проверьте, куда установились файлы PostgreSQL
ls -la /usr/pgsql-*/share/extension/*postgis*
ls -la /usr/pgsql-*/share/contrib/
ls -la /usr/pgsql-*/lib/*postgis*

# 7. Скопируйте файлы в директории YugabyteDB
cp -r /usr/pgsql-15/share/extension/*postgis* /home/yugabyte/share/postgresql/extension/
cp -r /usr/pgsql-15/share/contrib/postgis-* /home/yugabyte/share/postgresql/contrib/
cp /usr/pgsql-15/lib/postgis-*.so /home/yugabyte/lib/postgresql/

# 8. Выйдите из root и проверьте как пользователь yugabyte
exit
ls -la /home/yugabyte/share/postgresql/extension/ | grep postgis

# 9. Попробуйте создать расширение
ysqlsh -U yugabyte -d geo_test -c "CREATE EXTENSION postgis; SELECT postgis_version();"
```

### 📊 Что вы узнаете из этого эксперимента:

| Команда | Что показывает | Ключевая информация для Dockerfile |
|---------|----------------|-----------------------------------|
| `yum search postgis` | Доступные версии | Точное имя пакета для `RUN yum install -y ...` |
| `find /usr -name "*postgis*"` | Пути к файлам | Какие именно файлы нужно копировать |
| Пути `/usr/pgsql-*/` | Версия PostgreSQL | Нужно ли `15` или другая версия в путях |
| Тест создания расширения | Работает ли PostGIS | Нужны ли дополнительные настройки |

### ⚠️ Критические моменты для отслеживания:

1. **Версия PostgreSQL** — YugabyteDB 2.25 совместима с PostgreSQL 15, поэтому ищем пакеты для `_15`
2. **Права доступа** — файлы в `/home/yugabyte/` должны быть доступны пользователю `yugabyte`
3. **Зависимости** — если `yum install` просит установить дополнительные пакеты, их тоже нужно добавить в Dockerfile
4. **Конфликты версий** — если уже есть старые версии PostGIS, может потребоваться удалить их

### ✅ Преимущества ручной отладки:

1. **Быстрее в 3-5 раз** чем цикл "собрать образ → запустить → проверить → исправить"
2. **Понятные ошибки** — вы видите конкретное сообщение от yum, а не общую ошибку сборки
3. **Меньше "магических" команд** — вы понимаете каждое действие

### 🚀 После успешной ручной установки:

Когда вы найдёте рабочий набор команд в контейнере, просто **перенесите их в Dockerfile**:

```dockerfile
FROM yugabytedb/yugabyte:latest
USER root
RUN yum update -y
RUN yum install -y https://download.postgresql.org/pub/repos/yum/reporpms/EL-8-x86_64/pgdg-redhat-repo-latest.noarch.rpm
RUN yum install -y postgis31_15  # ИЛИ ТОЧНОЕ ИМЯ, КОТОРОЕ ВЫ НАШЛИ
RUN cp -r /usr/pgsql-15/share/extension/*postgis* /home/yugabyte/share/postgresql/extension/
RUN cp -r /usr/pgsql-15/share/contrib/postgis-3.1 /home/yugabyte/share/postgresql/contrib/
RUN cp /usr/pgsql-15/lib/postgis-3.1.so /home/yugabyte/lib/postgresql/
USER yugabyte
```

**Начните с ручной установки прямо сейчас.** Покажите вывод ключевых команд:
1. `yum search postgis`
2. Результат установки `yum install -y postgis...`
3. Пути к файлам PostGIS (`find /usr -name "*postgis*"`)

Это даст нам полную картину для создания идеального Dockerfile.
