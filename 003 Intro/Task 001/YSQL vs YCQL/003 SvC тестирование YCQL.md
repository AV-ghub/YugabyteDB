Для YCQL нагрузочный тест будет другим, так как здесь модель данных и сильные стороны — в быстрых операциях по ключу и работе с полуструктурированными данными. Я подготовил набор запросов, которые демонстрируют ключевые особенности YCQL, и сценарий нагрузочного теста, аналогичный вашему YSQL-тесту, но адаптированный под философию Cassandra-подобных баз.

### 📊 Набор демонстрационных запросов для YCQL

Выполните эти запросы в `ycqlsh` после создания клюспейса `demo` (`CREATE KEYSPACE IF NOT EXISTS demo WITH REPLICATION = {'class': 'SimpleStrategy', 'replication_factor': 3};`).

```sql
-- 1. СОСТАВНОЙ ПЕРВИЧНЫЙ КЛЮЧ и CLUSTERING ORDER (основа модели YCQL)
-- Создадим таблицу временных событий, где данные автоматически сортируются по времени для каждого устройства
CREATE TABLE IF NOT EXISTS demo.device_telemetry (
    device_id text,
    event_time timestamp,
    sensor_id int,
    readings map<text, double>, -- Документная модель внутри колонки
    PRIMARY KEY ((device_id), event_time, sensor_id) -- device_id - ключ партиции, остальное - для сортировки
) WITH CLUSTERING ORDER BY (event_time DESC, sensor_id ASC);

-- 2. ВСТАВКА данных с использованием коллекций (MAP)
INSERT INTO demo.device_telemetry (device_id, event_time, sensor_id, readings)
VALUES ('device_001', toTimestamp(now()), 1, {'temperature': 23.5, 'voltage': 3.7});

-- 3. ВЫБОРКА по ПАРТИЦИОННОМУ КЛЮЧУ (самая эффективная операция)
-- Получим все события для device_001, отсортированные по времени (от новых к старым, из-за CLUSTERING ORDER)
SELECT * FROM demo.device_telemetry WHERE device_id = 'device_001';

-- 4. ВЫБОРКА с ФИЛЬТРАЦИЕЙ по CLUSTERING COLUMN (эффективно из-за сортировки)
-- Получим события device_001 за последний час
SELECT * FROM demo.device_telemetry WHERE device_id = 'device_001' AND event_time > '2024-01-01 10:00:00-0000';

-- 5. ИСПОЛЬЗОВАНИЕ ВСТРОЕННЫХ ФУНКЦИЙ для генерации UUID
-- TTL (Time-To-Live) - автоматическое удаление устаревших данных
CREATE TABLE demo.user_sessions (
    user_id text,
    session_id timeuuid,
    data text,
    PRIMARY KEY (user_id, session_id)
) WITH default_time_to_live = 3600; -- Данные удалятся через час
INSERT INTO demo.user_sessions (user_id, session_id, data) VALUES ('user1', now(), 'active');

-- 6. BATCH-ОПЕРАЦИЯ: атомарная запись в несколько строк ОДНОЙ партиции
-- Это гарантирует атомарность на уровне партиции (отличительная возможность YCQL)
BEGIN BATCH
    INSERT INTO demo.device_telemetry (device_id, event_time, sensor_id, readings) VALUES ('device_001', toTimestamp(now()), 2, {'status': 1});
    INSERT INTO demo.device_telemetry (device_id, event_time, sensor_id, readings) VALUES ('device_001', toTimestamp(now()), 3, {'status': 0});
APPLY BATCH;

-- 7. ИНДЕКСИРОВАНИЕ по НЕКЛЮЧЕВОМУ ПОЛЮ (осторожно, может быть дорогим)
CREATE INDEX IF NOT EXISTS ON demo.device_telemetry (sensor_id);
-- Поиск по sensor_id теперь возможен, но сканирует всю таблицу
SELECT * FROM demo.device_telemetry WHERE sensor_id = 1 LIMIT 5;

-- 8. МАТЕРИАЛИЗОВАННЫЕ ПРЕДСТАВЛЕНИЯ (деноормализация для скорости чтения)
-- Создадим "перевернутую" таблицу для быстрого поиска устройств по sensor_id
CREATE MATERIALIZED VIEW demo.sensor_to_device AS
    SELECT sensor_id, device_id, event_time, readings
    FROM demo.device_telemetry
    WHERE sensor_id IS NOT NULL AND device_id IS NOT NULL AND event_time IS NOT NULL
    PRIMARY KEY ((sensor_id), device_id, event_time); -- Теперь sensor_id - ключ партиции
-- Запрос к представлению будет быстрым
SELECT * FROM demo.sensor_to_device WHERE sensor_id = 1;
```

### ⚡ Нагрузочный тест для YCQL (аналог вашего YSQL-теста)

Этот тест создает большой объем данных и проверяет скорость **точечных чтений и записей по ключу** — то, для чего оптимизирован YCQL.

```sql
-- 1. Создаем таблицу для нагрузочного теста
CREATE KEYSPACE IF NOT EXISTS stress WITH REPLICATION = {'class': 'SimpleStrategy', 'replication_factor': 3};
USE stress;

DROP TABLE IF EXISTS ycql_perf_test;
CREATE TABLE ycql_perf_test (
    bucket int,          -- "Ведро" для распределения данных (аналог партиции)
    item_id text,        -- Уникальный ID элемента в ведре
    value double,
    payload text,
    PRIMARY KEY ((bucket, item_id)) -- Ключ партиции = bucket + item_id
);

-- 2. Генерируем 1,000,000 записей (это может занять пару минут)
--    Используем 1000 "ведер" (bucket) для равномерного распределения данных по узлам
INSERT INTO stress.ycql_perf_test (bucket, item_id, value, payload)
VALUES (1, 'item_1', 0.5, 'Lorem ipsum dolor sit amet'); -- Первая запись

-- Для массовой вставки нужно использовать драйвер. В ycqlsh можно эмулировать циклом:
-- ВАЖНО: В реальном тесте используйте yugabyted load или свой скрипт. Это для демо:
BEGIN BATCH
INSERT INTO ycql_perf_test (bucket, item_id, value, payload) VALUES (1, 'item_1', 0.5, 'payload1');
INSERT INTO ycql_perf_test (bucket, item_id, value, payload) VALUES (1, 'item_2', 1.5, 'payload2');
APPLY BATCH;

-- 3. ТЕСТ 1: Молниеносное точечное чтение по ключу (сильная сторона YCQL)
-- Включим трассировку, чтобы увидеть время выполнения
TRACING ON;
SELECT * FROM ycql_perf_test WHERE bucket = 100 AND item_id = 'item_500';
TRACING OFF;

-- 4. ТЕСТ 2: Запись/обновление по ключу
TRACING ON;
UPDATE ycql_perf_test SET value = 99.9 WHERE bucket = 100 AND item_id = 'item_500';
TRACING OFF;

-- 5. ТЕСТ 3: Чтение ВСЕХ записей из одной партиции (также быстро)
-- Получим все item_id для bucket=100 (лимит 1000 для безопасности)
SELECT item_id, value FROM ycql_perf_test WHERE bucket = 100 LIMIT 1000;

-- 6. ТЕСТ 4 (Показательный): Попытка агрегации БЕЗ ключа партиции - будет МЕДЛЕННО
-- Этим мы покажем разницу с YSQL. YCQL не оптимизирован для полного сканирования.
-- Включим трассировку и увидим, что запрос выполняется долго или с таймаутом.
TRACING ON;
SELECT bucket, COUNT(*) FROM ycql_perf_test GROUP BY bucket LIMIT 5;
TRACING OFF;
```

### 📈 Анализ результатов и отличие от YSQL

После выполнения нагрузочного теста посмотрите на вывод `TRACING ON`. Вы увидите:
*   **Время выполнения** для каждого запроса (обычно **менее 10 мс** для операций по ключу).
*   **Количество обрабатываемых строк** (`rows fetched from storage`).

**Ключевое отличие от вашего YSQL-теста:**
*   **YSQL** блестяще справляется с агрегациями по всей таблице (`GROUP BY category`), так как это реляционная модель, оптимизированная для сложных запросов.
*   **YCQL** показывает максимальную скорость на **точечных операциях по известному ключу** (`WHERE bucket=... AND item_id=...`), но **не предназначен для полнотабличных агрегаций** (последний тест это наглядно покажет). Такие запросы в YCQL будут медленными, так как требуют полного сканирования всех узлов кластера.

**Какой вывод сделать за 2 часа?** Используйте YCQL, когда ваш доступ к данным в 90% случаев происходит по **известному ключу** (например, получить заказ по ID, сессию пользователя, показания датчика устройства). Если же нужны сложные аналитические запросы, джойны и агрегации по разным полям — это сфера YSQL.

Для более реалистичного нагрузочного теста с миллионом записей лучше написать небольшой скрипт на Python с использованием драйвера `cassandra-driver`, но для ознакомительных целей и демонстрации философии YCQL приведенного выше сценария достаточно. Успешного тестирования!


---------------------------------------------


Отлично, мы докопались до ключевой особенности! Ошибка показывает, что синтаксис `BEGIN TRANSACTION BATCH` не поддерживается напрямую в `ycqlsh`. В YugabyteDB YCQL транзакции на уровне партиции реализуются **только через prepared statements и драйвер приложения** (например, на Java, Python), а не через интерактивный `ycqlsh`.

Это важное ограничение, которое показывает разницу между "классическим CQL" и его реализацией в YugabyteDB. Давайте обойдем это и продолжим изучение другими показательными тестами, которые точно сработают в CLI.

### 🎯 Альтернатива и рабочие тесты для ycqlsh

Поскольку `BATCH` в `ycqlsh` не работает как транзакция, просто выполните вставки по отдельности — для нашего демо-теста это не критично. Главное — понять концепцию.

**Давайте переключимся на другие мощные и показательные особенности YCQL, которые отлично работают в CLI:**

1.  **Тест на скорость точечных вставок и чтений** (сильная сторона YCQL):
    ```sql
    -- Вставьте 1000 записей в цикле (эмуляция нагрузки)
    -- В ycqlsh нет циклов, но можно использовать Python или просто вставить несколько
    INSERT INTO test_demo.device_telemetry (device_id, event_time, sensor_id, readings) VALUES ('load_test', toTimestamp(now()), 100, {'temp': 25.5});
    -- Повторите эту команду несколько раз, меняя sensor_id

    -- Мгновенное чтение по ключу партиции (device_id) + clustering column (event_time)
    SELECT * FROM test_demo.device_telemetry WHERE device_id = 'load_test' ORDER BY event_time DESC LIMIT 5;
    ```

2.  **Демонстрация TTL (Time-To-Live) — автоудаление данных**:
    ```sql
    -- Создадим таблицу, где строки живут 30 секунд
    CREATE TABLE test_demo.temporary_data (
        id text PRIMARY KEY,
        value text
    ) WITH default_time_to_live = 30;

    -- Вставим данные и посмотрим, как они исчезнут
    INSERT INTO test_demo.temporary_data (id, value) VALUES ('test1', 'I will vanish soon');
    SELECT * FROM test_demo.temporary_data; -- Увидим запись
    -- Подождите 30+ секунд и выполните снова:
    SELECT * FROM test_demo.temporary_data; -- Данных нет!
    ```

3.  **Работа со сложными типами данных (MAP, SET, LIST)**:
    ```sql
    -- Создадим таблицу с коллекциями
    CREATE TABLE test_demo.user_profile (
        username text PRIMARY KEY,
        emails set<text>,
        preferences map<text, boolean>,
        recent_logins list<timestamp>
    );

    -- Вставка со сложными типами
    INSERT INTO test_demo.user_profile (username, emails, preferences, recent_logins)
    VALUES ('jdoe', {'jdoe@mail.com', 'john@work.com'}, {'email_notifications': true, 'dark_mode': false}, [toTimestamp(now())]);

    -- Обновление коллекций
    UPDATE test_demo.user_profile SET emails = emails + {'new@mail.com'} WHERE username = 'jdoe';
    SELECT * FROM test_demo.user_profile;
    ```

4.  **Материализованное представление (денорамиализация для скорости)**:
    ```sql
    -- Создадим MV для быстрого поиска телеметрии по sensor_id
    CREATE MATERIALIZED VIEW test_demo.telemetry_by_sensor AS
        SELECT sensor_id, device_id, event_time, readings
        FROM test_demo.device_telemetry
        WHERE sensor_id IS NOT NULL AND device_id IS NOT NULL AND event_time IS NOT NULL
        PRIMARY KEY ((sensor_id), device_id, event_time);

    -- Теперь быстрый поиск по sensor_id (который стал ключом партиции в MV)
    SELECT * FROM test_demo.telemetry_by_sensor WHERE sensor_id = 100;
    ```

5.  **Проверка консистентности чтения (отличие от eventual consistency Cassandra)**:
    ```sql
    -- В YCQL по умолчанию STRONG consistency. Проверим:
    CONSISTENCY ALL; -- Установим максимальный уровень (чтение со всех реплик)
    INSERT INTO test_demo.consistency_test (id, value) VALUES ('test', 'value');
    -- Немедленное чтение всегда вернет актуальные данные
    SELECT * FROM test_demo.consistency_test WHERE id = 'test';
    ```

### 📊 Итог: что мы узнали об YCQL

Из этой ошибки с `BATCH` вытекает важный вывод для архитектуры:

| Особенность | YCQL в YugabyteDB | Классическая Apache Cassandra |
|-------------|-------------------|-------------------------------|
| **Транзакции** | Поддерживаются на уровне **одной партиции**, но требуют драйвера приложения | Нет настоящих ACID-транзакций |
| **Согласованность** | **Strong по умолчанию** (линейная) | Eventual consistency по умолчанию |
| **Интерфейс** | Транзакционный CQL через драйверы, ограниченный `ycqlsh` | Полный CQL через cqlsh |
| **Сильные стороны** | Быстрые операции по ключу + гарантии ACID + гибкая схема | Максимальная скорость записи и горизонтальное масштабирование |

### 🎯 Краткий план на оставшееся время

1.  **Проверьте TTL** — это наглядная демонстрация отличия от реляционной модели.
2.  **Создайте материализованное представление** — ключевой паттерн денормализации в YCQL.
3.  **Поработайте с коллекциями (MAP/SET)** — покажите гибкость схемы.

Эти три теста займут 15-20 минут и дадут полное представление о ключевых возможностях YCQL. Попробуйте их и сообщите, какие результаты получились — это поможет двигаться дальше к Docker-вопросу.
