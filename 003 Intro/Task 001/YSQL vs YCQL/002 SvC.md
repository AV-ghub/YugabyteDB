Проблема в том, что `ycqlsh` пытается подключиться к порту `9042` на `localhost` (`127.0.0.1`), но в контейнере TServer этот порт может слушать другой сетевой интерфейс или TServer не запущен. Давайте быстро продиагностируем и решим проблему.

### 🔍 Быстрая диагностика и решение

Выполните эти команды **внутри контейнера** `yugabyte_yb-tserver-a_1`.

1.  **Проверьте, запущен ли процесс TServer и слушает ли порт 9042:**
    ```bash
    # Проверим процессы
    ps aux | grep yb-tserver
    ```
    **Что ищем:** Вы должны увидеть строку с командой запуска `/home/yugabyte/bin/yb-tserver`. Если её нет, TServer не запущен.

2.  **Проверим, на каком IP и порту слушает TServer:**
    ```bash
    netstat -tlnp | grep 9042
    ```
    Ключевой вывод — в столбце `Local Address`. Если там указан **не** `0.0.0.0:9042`, а, например, `172.x.x.x:9042`, значит, процесс слушает на конкретном IP, а не на localhost.

3.  **Узнаем актуальный IP-адрес контейнера:**
    ```bash
    hostname -I
    ```
    Это покажет все IP-адреса контейнера. Обычно это что-то вроде `172.18.0.2` или `172.19.0.3`.

### 🚀 Как подключиться к YCQL

Используйте полученную информацию для подключения. **Самый вероятный и быстрый способ** — подключаться не к `localhost`, а к IP контейнера.

**Вариант 1 (Рекомендуемый): Подключение по IP контейнера**
```bash
# Замените 172.18.0.2 на реальный IP из команды 'hostname -I'
/home/yugabyte/bin/ycqlsh 172.18.0.2
```

**Вариант 2 (Если TServer слушает на localhost): Проверьте переменные окружения**
Иногда адрес указывается в переменной. Проверьте её и используйте:
```bash
echo $CQL_PROXY_BIND_ADDRESS
# Если она содержит IP (например, 172.18.0.2), используйте его
/home/yugabyte/bin/ycqlsh $CQL_PROXY_BIND_ADDRESS
```

**Вариант 3 (Если порт отличается): Подключение с явным указанием порта**
```bash
# Если netstat показал другой порт (маловероятно)
/home/yugabyte/bin/ycqlsh 172.18.0.2 <port_number>
```

### 💡 Почему это происходит в Docker

В Docker-композиции каждый сервис (контейнер) имеет свой собственный IP в виртуальной сети Docker. Процесс внутри контейнера может быть сконфигурирован слушать именно на этом IP для безопасности или корректной маршрутизации между контейнерами. Поэтому `localhost` (`127.0.0.1`) внутри контейнера может быть неправильным адресом для подключения.

### 📋 Краткий план действий

1.  **В контейнере** выполните `hostname -I` и запомните IP (например, `172.18.0.2`).
2.  **Попробуйте подключиться** командой: `/home/yugabyte/bin/ycqlsh 172.18.0.2`.
3.  **Если подключение не удалось**, проверьте статус TServer командой `ps aux | grep yb-tserver`. Если процесса нет, возможно, кластер не запущен корректно.

Скорее всего, после подключения по правильному IP всё заработает. Попробуйте и сообщите о результате. Если команда `ps aux` не покажет процесс `yb-tserver`, значит, проблема глубже, и нужно смотреть логи запуска кластера.
