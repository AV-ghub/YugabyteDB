За час-два вы получите отличный обзор. Два интерфейса в YugabyteDB — это не опциональные оболочки, а суть его гибридной архитектуры. Вот ключевые моменты.

### 🗺️ Быстрая карта: два API в YugabyteDB
Для наглядности, основные различия сведены в таблицу.

| Аспект | **YSQL (PostgreSQL API)** | **YCQL (Cassandra API)** |
| :--- | :--- | :--- |
| **Модель данных** | Реляционная (таблицы, связи, схемы) | Полуструктурированная (документы, коллекции, ключ-значение) |
| **Запросы** | Полный SQL (JOIN, подзапросы, транзакции) | CQL (оптимизирован для быстрого доступа по ключу) |
| **Транзакции** | Полноценные ACID, включая распределённые | ACID для одной строки/партиции; NoSQL для распределённых |
| **Идеальные сценарии** | Комплексные OLTP, финансовые системы, замена PostgreSQL | Высокопроизводительный OLTP, сессии, каталоги, IoT |
| **Концепция** | **Мощь PostgreSQL** с горизонтальным масштабированием | **Гибкость Cassandra** с гарантированной консистентностью |

### 🤔 Зачем два интерфейса? Идеология YugabyteDB
Главная идея YugabyteDB — объединить достоинства SQL и NoSQL в одной системе. И **YSQL**, и **YCQL** работают поверх единого распределённого слоя хранения **DocDB**.
*   **Почему два?** Чтобы покрыть максимальное число сценариев и снизить порог входа. Команде, мигрирующей с PostgreSQL, не нужно переписывать приложения. А разработчикам NoSQL-приложений не придётся мириться с eventual consistency, если нужна гарантированная целостность.
*   **Корреляция данных:** Данные, созданные через YSQL, **недоступны** напрямую через YCQL, и наоборот. Это разные модели, работающие с одними и теми же физическими ресурсами, но через разные «окна».

### 🔧 Как зайти в YCQL через CLI и что проверить
1.  **Подключитесь к контейнеру YB-TServer** (как вы уже делали):
    ```bash
    docker exec -it yugabyte_yb-tserver-a_1 bash
    ```
2.  **Запустите CLI (`ycqlsh`)**. Это аналог `psql`, но для YCQL.
    ```bash
    /home/yugabyte/bin/ycqlsh
    ```
3.  **Быстрая диагностика возможностей YCQL** (в сессии `ycqlsh`):
    ```sql
    -- 1. Создайте keyspace (аналог базы данных)
    CREATE KEYSPACE IF NOT EXISTS test_demo WITH REPLICATION = { 'class' : 'SimpleStrategy', 'replication_factor' : 3 };

    -- 2. Используйте его и создайте таблицу с составным ключом
    USE test_demo;
    CREATE TABLE user_sessions (
        user_id text,
        session_id timeuuid,
        data map<text, text>,
        PRIMARY KEY (user_id, session_id)
    ) WITH CLUSTERING ORDER BY (session_id DESC);

    -- 3. Вставьте и прочитайте данные
    INSERT INTO user_sessions (user_id, session_id, data) VALUES ('user1', now(), {'page': '/home', 'status': 'active'});
    SELECT * FROM user_sessions WHERE user_id = 'user1';

    -- 4. Проверьте возможности, которые отличают YCQL от классической Cassandra:
    --    Пробуйте использовать встроенные функции (например, `now()`, `uuid()`).
    --    Попробуйте выполнить транзакцию на уровне одной партиции (вставка в несколько строк с одним `user_id` в одном batch-запросе).
    ```
    Ключевые отличительные возможности YCQL, которые стоит проверить:
    *   **Документная модель внутри таблиц**: Использование типов `MAP`, `SET`, `LIST` в столбцах.
    *   **Гибкое распределение**: Составные первичные ключи (`PRIMARY KEY (user_id, session_id)`) для контроля партиционирования и сортировки.
    *   **Согласованность по умолчанию**: YCQL по умолчанию использует **строгую согласованность (strong consistency)** для чтения и записи, а не eventual consistency, как Apache Cassandra. Это важнейшее отличие.

### ✅ Итог: бестпрактис и оценка за 1-2 часа
*   **Краткий вывод**: YSQL — для сложных запросов и транзакций. YCQL — для быстрых операций по ключу с гибкой схемой. Оба — часть одной распределённой системы.
*   **Бестпрактис для старта**: Если ваше приложение «мыслит» таблицами и связями — стартуйте с **YSQL**. Если данные — это потоки событий, сессии или документы с простыми запросами по ключу — используйте **YCQL**. Это сэкономит время на разработке.
*   **Что изучить за оставшееся время:** После базовых команд сосредоточьтесь на консоли YugabyteDB (`http://<node-ip>:7000`). Там вы увидите метрики производительности и распределения данных для **обоих API**, что поможет понять, как они сосуществуют.

Попробуйте эти шаги с `ycqlsh`. Если у вас возникнут конкретные трудности при подключении или проверке функций, напишите — помогу их быстро решить.
