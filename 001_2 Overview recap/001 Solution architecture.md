Последовательно разберем вопросы по архитектуре и работе кластера YugabyteDB, опираясь на официальную документацию и практические наблюдения.

Для наглядности ключевые концепции сведены в таблицу:

| Концепция | Суть | Практическое значение |
| :--- | :--- | :--- |
| **Таблет (Tablet)** | Шард данных, на который делится таблица. Один из фундаментальных объектов распределения. | Вся работа (чтение/запись, репликация, отказоустойчивость) происходит на уровне таблетов. |
| **Группа реплик таблета (Raft Group)** | Набор из нескольких идентичных копий (реплик) одного таблета, размещенных на разных узлах. | Обеспечивает доступность данных и отказоустойчивость через механизм консенсуса Raft. |
| **Replication Factor (RF)** | Количество реплик (копий) каждого таблета в кластере. | Определяет отказоустойчивость: **RF=3** позволяет пережить сбой до 2 узлов (при условии, что они не хранят одни и те же данные), **RF=1** отказоустойчивости не дает. |
| **Лидер таблета (Tablet Leader)** | Одна реплика в группе, избранная для обслуживания всех операций чтения и записи для своего таблета. | Координирует все транзакции, обеспечивая строгую согласованность данных. |
| **Мастер-сервер (YB-Master)** | Управляет **метаданными** кластера: каталог таблиц, схема распределения таблетов по TServer, информация о системном состоянии. | Не хранит пользовательские данные и не участвует в обработке запросов. Является единой точкой управления кластером. |

Теперь давайте ответим на ваши вопросы.

### ❓ Природа "лагов" при сбое узла
При остановке TServer-узла происходит не мгновенное переключение, а **последовательный процесс**:
1.  **Обнаружение сбоя**: оставшиеся узлы перестают получать heartbeat-сигналы от вышедшего из строя.
2.  **Выбор нового лидера**: Для каждого таблета, лидер которого находился на упавшем узле, в его группе реплик запускается выбор нового лидера по протоколу Raft.
3.  **Перестройка кворума**: Новая реплика-лидер должна собрать кворум (большинство голосов) из оставшихся живых реплик своей группы.

Это **операция на уровне метаданных и консенсуса**. Продолжительность лага **практически не зависит от объема пользовательских данных** в таблице и в основном определяется таймаутами сети и выборами лидера (RTO обычно составляет около 3 секунд). Однако если в момент сбоя шли активные записи, они будут поставлены в очередь и завершены новым лидером, что может добавить дополнительную задержку.

### 🎯 Роль мастера и необходимость нескольких мастеров
*   **Роль мастера (YB-Master)**: это **"мозг"** кластера. Он хранит системный каталог (какие таблицы существуют, как они разбиты на таблеты, где находятся реплики каждого таблета). TServer-узлы обращаются к мастеру, чтобы узнать, за какие таблеты они отвечают и где найти лидеров для других таблетов.
*   **Необходимость нескольких мастеров**: **Да, это критически важно для отказоустойчивости самой системы управления.** Мастер — это тоже процесс, который может упасть. В production-развертывании всегда запускают **нечетное количество мастеров (обычно 3)**. Они образуют свою собственную группу репликации Raft для метаданных. Это гарантирует, что даже при падении одного мастер-сервера кластер сохранит целостность системного каталога и сможет управлять собой.

### 🔍 Просмотр и управление распределением данных
Чтобы увидеть, как распределена конкретная таблица, можно использовать веб-интерфейс или SQL-запросы:
*   **Веб-интерфейс (`http://localhost:7000`)**: На вкладке **Tables** выберите нужную таблицу, затем перейдите на вкладку **Tablets**. Вы увидите список всех таблетов таблицы и узлы, на которых расположены их реплики.
*   **SQL-функция**: Выполните запрос `SELECT * FROM yb_servers();` чтобы увидеть список всех узлов кластера.
*   **CLI-команда**: Через `yb-admin list_tablets <keyspace> <table_name>` можно получить детальную информацию.

**Повлиять на распределение можно несколькими способами**:
1.  **Разметка кластера (Placement Info)**: При создании кластера можно указать "облака", "регионы" и "зоны" для каждого узла. Затем, задавая политику размещения (`placement policy`), вы указываете, в каких зонах и в каком количестве должны находиться реплики данных. Это продвинутая настройка для географического распределения.
2.  **Стратегия шардирования таблицы**: При создании таблицы можно выбрать, как она будет разбиваться на таблеты:
    *   `HASH` (по умолчанию): Равномерное распределение. Подходит для большинства сценариев.
    *   `RANGE`: Данные сортируются по первичному ключу. Полезно для запросов по диапазонам.
    *   Задать количество таблетов (`SPLIT INTO ... TABLETS`).

### 📚 Рекомендуемые ресурсы для углубления
*   **Документация по репликации в DocDB**: Объясняет ядро процесса — репликацию на уровне таблетов.
*   **Конфигурация yb-master**: Подробно описывает все флаги и роль мастера.
*   **Планирование кластера**: Дает рекомендации по выбору топологии, отказоустойчивости и размеров для production.

