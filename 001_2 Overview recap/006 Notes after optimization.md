```
версия с тремя мастерами не взлетела
убрал как было
остальные параметры прошли без проблем
на старте потребление памяти не изменилось
не понятно. зачем мы вызываем в конце
-- Проверяем использование кэша
SELECT * FROM yb_servers();
он же не показывает ничего особо значимого?
    host     | port | num_connections | node_type | cloud  |   region    | zone  | public_ip |               uuid               |            universe_uuid
-------------+------+-----------------+-----------+--------+-------------+-------+-----------+----------------------------------+--------------------------------------
 yb-tserver3 | 5433 |               0 | primary   | cloud1 | datacenter1 | rack1 |           | 1f60f7181f7f45d0a0213372dba46630 | bf1f9b8a-6928-4972-81c2-2b58a0050980
 yb-tserver2 | 5433 |               0 | primary   | cloud1 | datacenter1 | rack1 |           | 3db5bdd1a97d49f2be9a8c208182953c | bf1f9b8a-6928-4972-81c2-2b58a0050980
 yb-tserver1 | 5433 |               0 | primary   | cloud1 | datacenter1 | rack1 |           | a7b32be2a5554c6db54a29ae3eeedfc2 | bf1f9b8a-6928-4972-81c2-2b58a0050980
(3 rows)
что мы можем взять из этой информации?
```
Отличные наблюдения! Давайте разберем оба момента.

## 🔧 Почему не взлетели 3 мастера и почему память не изменилась

### 1. Проблема с 3 мастерами
Типичные причины и решение:

```bash
# Проверьте логи мастера, который не запускается
docker-compose logs yb-master-b --tail 30

# Частая ошибка: конфликт портов в сети Docker
# Решение 1: Убедитесь, что все мастеры в ОДНОЙ сети
docker network ls
docker network inspect <network_name>

# Решение 2: Добавьте healthcheck для мастеров в конфиг
yb-master:
  healthcheck:
    test: ["CMD", "yb-admin", "--master_addresses", "yb-master:7100,yb-master-b:7100,yb-master-c:7100", "list_all_masters"]
    interval: 10s
    timeout: 5s
    retries: 10
```

### 2. Почему память не изменилась
**Потому что `mem_limit` в Docker ≠ `--memory_limit_hard_bytes` в YugabyteDB!**

Это критически важный момент:

| Параметр | Где указан | Что делает | Влияет на что |
|----------|------------|------------|---------------|
| **`mem_limit`** | `docker-compose.yml` | Лимит Docker на контейнер | Память ВСЕГО контейнера (OS + Yugabyte + буферы) |
| **`--memory_limit_hard_bytes`** | `command:` в конфиге | Лимит памяти для процесса yb-tserver | Только рабочая память YugabyteDB |

**Проверьте реальное потребление:**
```bash
# 1. Посмотрите фактическое потребление Docker
docker stats --no-stream --format "table {{.Name}}\t{{.MemUsage}}\t{{.MemPerc}}"

# 2. Проверьте, видят ли TServer'ы новый лимит
docker exec yugabyte_yb-tserver-a_1 ps aux | grep -A5 "yb-tserver"
# В выводе должна быть строка с --memory_limit_hard_bytes=...

# 3. Проверьте через веб-интерфейс
# Откройте http://localhost:7000 → вкладка "Tablet Servers"
# Колонка "Memory" должна показывать ~32GB Used
```

Если память все еще не изменилась, возможно **неправильный синтаксис**:
```yaml
# Правильно (обратите внимание на кавычки для больших чисел):
command:
  - "--memory_limit_hard_bytes=34359738368"  # 32GB в БАЙТАХ
```

## 🔍 Что показывает `yb_servers()` и зачем он нужен

Вы правы — в обычном состоянии он показывает базовую информацию. Но его **реальная ценность** раскрывается при диагностике:

### 1. **Понимание топологии кластера**
```sql
-- Видим: 3 узла, все в одной зоне (rack1)
-- В production это было бы плохо — все узлы в одной стойке!
-- Идеально: cloud1:region1:zone1, cloud1:region1:zone2, cloud1:region1:zone3

SELECT 
    host, 
    cloud, 
    region, 
    zone,
    CASE 
        WHEN zone = 'rack1' THEN '⚠️ ВСЕ в одной зоне - риск!'
        ELSE '✅ OK'
    END as placement_status
FROM yb_servers();
```

### 2. **Диагностика распределения нагрузки**
```sql
-- Создадим представление для мониторинга
CREATE VIEW node_load AS
SELECT 
    host,
    (SELECT COUNT(*) FROM pg_stat_activity WHERE client_addr = host) as active_connections,
    (SELECT COUNT(*) FROM yb_table_properties() WHERE tablet_leader LIKE host || '%') as leader_tablets,
    (SELECT COUNT(*) FROM yb_table_properties() WHERE tablet_replicas LIKE '%' || host || '%') as total_tablets
FROM yb_servers();

-- Проверим балансировку
SELECT * FROM node_load;
```

### 3. **Отслеживание перераспределения таблетов**
```sql
-- После остановки узла выполните:
SELECT * FROM yb_servers();
-- И сравните с предыдущим состоянием
-- Изменение в node_type или исчезновение узла = проблемы
```

### 4. **Проверка работы с кэшем** (косвенная)
```sql
-- Связываем с метриками производительности
SELECT 
    s.host,
    s.zone,
    (SELECT SUM(heap_blks_hit) FROM pg_statio_user_tables) as cache_hits,
    (SELECT SUM(heap_blks_read) FROM pg_statio_user_tables) as disk_reads,
    ROUND(
        (SELECT SUM(heap_blks_hit))::numeric / 
        NULLIF((SELECT SUM(heap_blks_hit) + SUM(heap_blks_read)), 0) * 100, 
        2
    ) as cache_hit_ratio
FROM yb_servers() s;
```

## 🎯 Практическое применение информации из `yb_servers()`

Из вашего вывода мы узнаем:

1. **Узлы называются `yb-tserver1`, `yb-tserver2`, `yb-tserver3`** — значит Docker переименовал их
2. **Все в одной зоне (`rack1`)** — для тестов нормально, для production плохо
3. **`num_connections = 0`** — к кластеру сейчас никто не подключен
4. **Есть `universe_uuid`** — идентификатор вашего кластера

## 📊 Полезные запросы для мониторинга

```sql
-- 1. Распределение лидер-таблетов по узлам
SELECT 
    split_part(tablet_leader, ':', 1) as host,
    COUNT(*) as leader_count
FROM yb_table_properties()
GROUP BY 1
ORDER BY 2 DESC;

-- 2. Проверка использования памяти (если включены расширения)
SELECT * FROM yb_pg_metrics;

-- 3. Самые "горячие" таблицы
SELECT 
    schemaname, 
    tablename,
    seq_scan,
    idx_scan,
    n_tup_ins as inserts,
    n_tup_upd as updates,
    n_tup_del as deletes
FROM pg_stat_user_tables
ORDER BY (n_tup_ins + n_tup_upd + n_tup_del) DESC
LIMIT 10;
```

## 🔧 Быстрая диагностика памяти

```bash
# 1. Проверьте, применились ли настройки
docker exec yugabyte_yb-tserver-a_1 grep -r "memory_limit" /home/yugabyte/var/logs/

# 2. Альтернативный способ установки лимита
command:
  - "--memory_limit_hard_bytes=$$((32 * 1024 * 1024 * 1024))"  # 32GB через вычисление

# 3. Если не помогает, используйте переменные окружения
environment:
  - YB_TSERVER_MEMORY_LIMIT=32G
```
