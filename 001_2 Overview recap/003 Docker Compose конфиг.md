### docker-compose.yml
```yaml
# docker-compose.yml
version: '3.8'

services:
  # Главный узел управления
  yb-master:
    image: yugabytedb/yugabyte:latest
    command:
      - "/home/yugabyte/bin/yb-master"
      - "--fs_data_dirs=/mnt/master"
      - "--master_addresses=yb-master:7100"
      - "--rpc_bind_addresses=yb-master:7100"
      - "--replication_factor=1"  # КРИТИЧЕСКИ ВАЖНО для локального теста!
    mem_limit: 1024m
    ports:
      - "7000:7000"  # Веб-интерфейс

  # Три узла данных (TServer)
  yb-tserver1:
    image: yugabytedb/yugabyte:latest
    command:
      - "/home/yugabyte/bin/yb-tserver"
      - "--fs_data_dirs=/mnt/tserver"
      - "--tserver_master_addrs=yb-master:7100"
      - "--rpc_bind_addresses=yb-tserver1:9100"
      - "--enable_ysql"
      - "--ysql_enable_auth=false"
      - "--pgsql_proxy_bind_address=0.0.0.0:5433"
    ports:
      - "5433:5433"  # PostgreSQL-совместимый порт
    depends_on:
      - yb-master
    mem_limit: 2048m  # Минимум 2 ГБ!

  yb-tserver2:
    image: yugabytedb/yugabyte:latest
    command:
      - "/home/yugabyte/bin/yb-tserver"
      - "--fs_data_dirs=/mnt/tserver"
      - "--tserver_master_addrs=yb-master:7100"
      - "--rpc_bind_addresses=yb-tserver2:9100"
      - "--enable_ysql"
      - "--ysql_enable_auth=false"
    depends_on:
      - yb-master
    mem_limit: 2048m

  yb-tserver3:
    image: yugabytedb/yugabyte:latest
    command:
      - "/home/yugabyte/bin/yb-tserver"
      - "--fs_data_dirs=/mnt/tserver"
      - "--tserver_master_addrs=yb-master:7100"
      - "--rpc_bind_addresses=yb-tserver3:9100"
      - "--enable_ysql"
      - "--ysql_enable_auth=false"
    depends_on:
      - yb-master
    mem_limit: 2048m
```

### ⚙️ Параметры Мастера-узла (YB-Master)
Мастер — это «мозг» кластера, он управляет **метаданными**, но не хранит пользовательские данные.

| Параметр | Значение | Объяснение |
| :--- | :--- | :--- |
| `command: "/home/yugabyte/bin/yb-master"` | Исполняемый файл мастера. | Запускает процесс YB-Master. |
| `--fs_data_dirs=/mnt/master` | Директория для данных. | Здесь мастер хранит свои собственные системные данные (каталоги таблиц, информацию о таблетах). |
| `--master_addresses=yb-master:7100` | Список адресов всех мастеров в кластере. | **Ключевой параметр для кластера мастеров.** У нас только один мастер, но в продакшне указывают адреса 3-5 мастеров для кворума. |
| `--rpc_bind_addresses=yb-master:7100` | IP и порт для внутреннего RPC-общения. | Узел будет принимать внутренние запросы (от других мастеров и TServer) по этому адресу. |
| `--replication_factor=3` | Коэффициент репликации (Replication Factor, RF). | **Самая важная настройка для отказоустойчивости.** RF=3 означает, что каждый кусочек данных (таблет) будет храниться в 3 копиях на разных T-серверах. Это позволяет кластеру пережить сбой любого из трех узлов без потери данных и с минимальным простоем. |
| `mem_limit: 1024m` | Лимит оперативной памяти. | Для процесса мастера обычно хватает 1-2 ГБ. |
| `ports: - "7000:7000"` | Проброс порта веб-интерфейса. | **Веб-интерфейс (или Master UI)** по адресу `http://localhost:7000`. Здесь видна вся информация о кластере: узлы, таблицы, таблеты. |

### ⚙️ Параметры T-Серверов (YB-TServer)
T-серверы — это «мускулы» кластера. Они **хранят пользовательские данные**, обрабатывают SQL-запросы (через YSQL API) и работают с ними.

| Параметр | Значение (yb-tserver-a) | Объяснение |
| :--- | :--- | :--- |
| `command: "/home/yugabyte/bin/yb-tserver"` | Исполняемый файл T-сервера. | Запускает процесс YB-TServer. |
| `--fs_data_dirs=/mnt/tserver` | Директория для данных. | Здесь T-сервер хранит все пользовательские данные и журналы репликации. |
| `--tserver_master_addrs=yb-master:7100` | Адрес(а) мастера для подключения. | T-сервер подключается к мастеру, чтобы получить информацию о том, какие таблеты ему обслуживать, и узнать о состоянии кластера. |
| `--rpc_bind_addresses=yb-tserver-a:9100` | IP и порт для внутреннего RPC-общения. | Адрес, по которому этот T-сервер доступен для других узлов кластера. |
| `--enable_ysql` | Включение PostgreSQL-совместимого API (YSQL). | **Без этого флага вы не сможете подключаться через `psql` и использовать знакомый SQL.** |
| `--ysql_enable_auth=false` | Отключение аутентификации. | Для тестового стенда упрощает подключение (пароль не требуется). В production **обязательно нужно включать**. |
| `--pgsql_proxy_bind_address=0.0.0.0:5433` | Адрес для приема SQL-подключений. | **Критически важный порт.** Именно на него стучатся клиенты (psql, приложения). `0.0.0.0` означает "принимать подключения с любых адресов". |
| `ports: - "5433:5433"` | Проброс порта на хост. | Делает порт 5433 T-сервера доступным на localhost вашей машины. |
| `mem_limit: 2048m` | Лимит оперативной памяти. | Для T-сервера нужно минимум 2 ГБ для комфортной работы. |

> ⚠️ **Примечание про порты:** В нашем конфиге порт 5433 проброшен только у `yb-tserver-a`. Это создает единую точку входа. Остальные T-серверы (`b` и `c`) не проброшены наружу — они нужны только для репликации данных и отказоустойчивости внутри сети Docker. Чтобы подключиться к ним напрямую, нужно пробросить разные порты (например, 5434:5433 и 5435:5433) и использовать умный драйвер (Smart Driver) для балансировки.

### 🧩 Связка всего вместе: как это работает
1.  **Запуск**: Сначала стартует **мастер**, затем T-серверы. T-сервер регистрируется на мастере и говорит: «Я здесь, готов обслуживать таблеты».
2.  **Создание таблицы**: Когда вы создаете таблицу через YSQL, мастер определяет, на сколько таблетов её разбить, и назначает для каждого таблета группу из 3 реплик на разных T-серверах (согласно RF=3).
3.  **Запись данных**: При вставке строки, лидер-реплика соответствующего таблета записывает её в свой WAL (журнал) и реплицирует через **протокол Raft** на остальные реплики. Операция считается успешной, когда её подтвердит **кворум** (большинство) реплик (например, 2 из 3).
4.  **Чтение данных**: Запросы на чтение по умолчанию идут к лидер-реплике таблета для гарантии **строгой согласованности**.

### 🔍 Полезные ссылки на документацию
*   **Официальный справочник по yugabyted**: Общая утилита управления.
*   **Справочник по флагам YB-TServer**: Все параметры конфигурации T-сервера. Обратите внимание, что в Docker Compose мы указываем их в секции `command:`.
*   **Руководство по изменению конфигурации кластера**: Полезно, когда потребуется менять настройки или мигрировать узлы.
